# 개인정보

> 🌐 한국어 문서입니다. English version: [`main` 브랜치의 PRIVACY.md](https://github.com/songyb111-gachon/codex-auto-resume-windows/blob/main/PRIVACY.md)

Codex Auto Resume는 사용자의 PC에서 실행됩니다. 서버도, 계정도, 텔레메트리도 없으며, 워처는 스스로
바깥으로 나가는 네트워크 요청을 하지 않습니다.

예외가 하나 있고, 각주가 아니라 앞에서 밝히는 편이 맞습니다.
**Codex 플러그인으로 설치하거나 업데이트할 때 GitHub에서 릴리스를 내려받습니다.** 아래 절에서는 이 둘을
나누어 설명합니다. 실제로 다른 이야기이기 때문입니다.

## 개발자에게 무엇을 전송하는가

아무것도 전송하지 않습니다. 텔레메트리도, 분석도, 크래시 리포트도, 자동 업데이트 확인도 없으며 —
그것을 받을 엔드포인트 자체가 어디에도 없습니다.

구체적으로, 다음 중 어느 것도 이 PC를 떠나지 않습니다:

- 프롬프트, 그리고 대화 내용 일체;
- 어시스턴트 응답;
- 도구 입력과 도구 출력;
- 파일이나 저장소 내용;
- 계정 식별자, 이메일 주소, 구독 정보;
- 자격 증명, API 키, 토큰, 쿠키;
- 제공자가 보낸 오류 본문 원문;
- 대화 제목, 프로젝트 이름, 작업 디렉터리 경로;
- 대화 UUID, 복구 횟수, 그 밖에 무엇을 했는지에 관한 어떤 통계도.

이것은 정책이 아니라 코드의 속성입니다. `src/`나 `scripts/*.py` 아래의 어떤 코드도 네트워킹 모듈을
import하지 않으므로, 워처는 실수로라도 연결을 열 수 없습니다.

## 설치할 때

Codex 플러그인이 권장 설치 경로이고, 이 플러그인은 자립형이 아닙니다. 워처와 설정 창과 Windows 런타임은
릴리스 압축 파일에 담겨 옵니다. 그래서 Codex에게 auto resume을 설정해 달라고 하면
`scripts/bootstrap.ps1`이 그 압축 파일을 내려받습니다.

- HTTPS로 **github.com**과 GitHub의 릴리스 저장소에만 접속하며, 그 밖에는 어디에도 접속하지 않습니다.
- 내려받는 것은 정확히 두 가지입니다. 이 버전의 릴리스 압축 파일, 그리고 — 그 버전의 체크섬이
  플러그인에 고정되어 있지 않을 때에 한해 — 그 옆에 함께 게시된 `.sha256`입니다.
- URL은 저장소 안의 상수와 플러그인 자신의 버전으로 만듭니다. "latest"는 쓰지 않으며, 사용자가 입력한
  무엇도 URL의 일부가 되지 않습니다.
- 올려 보내는 것은 없습니다. 프롬프트, 대화, 계정 식별자, 기기 식별자, 복구 데이터 중 어느 것도 요청에
  붙지 않습니다. 갈 곳 자체가 없습니다.

따라서 GitHub는 여느 다운로드와 마찬가지로 평범한 다운로드 요청 하나를 보게 되고, 저장소의 공개 다운로드
집계에 그것을 더합니다. 이 도구를 설치하는 일이 눈에 띄지 않을 수 없는 지점은 이 하나이며, 여기서 어떤
표현을 쓰더라도 그 사실이 달라지지는 않습니다. 릴리스 압축 파일로 설치하는 경로는 이를 완전히 피합니다.
ZIP을 손에 넣은 뒤에는 `Install.cmd`가 네트워크를 전혀 쓰지 않습니다.

이 도구를 **사용하는** 과정에는 GitHub가 전혀 관여하지 않습니다. 업데이트를 확인하는 일도 없습니다.

## 무엇을 읽는가

작업이 중단되었다는 것을 알아채기 위해, Codex 자신의 로컬 상태를 읽기 전용으로 읽습니다:

- 수명 주기 기록 — 턴이 실패했는지, 그리고 Codex가 기록한 구조화된 오류 variant;
- 정확한 대화 UUID와 턴 번호;
- 리셋 시각을 얻기 위한 rate limit·사용량 스냅숏;
- `threads.name`, 프로젝트 이름, 작업 디렉터리 경로의 마지막 조각. 알림에 표시할 이름으로만 씁니다.

프롬프트가 담기는 필드는 의도적으로 읽지 않습니다. 현재 Codex 스키마에서 `title`, `preview`,
`first_user_message`는 첫 메시지 원문을 담고 있으므로 절대 읽지 않습니다. 표시용 이름은 오직
`threads.name`에서만 옵니다. 식별은 언제나 UUID로 하며, 표시용 이름으로 하지 않습니다.

Codex의 파일에는 절대 쓰지 않고, 그 파일에 잠금을 걸지도 않으며, 자격 증명 저장소는 절대 건드리지 않습니다.

## 무엇을 실행하는가

복구를 위해 시작하는 모든 프로세스는 이 PC에서, 이미 설치되어 있는 Codex를 대상으로 실행됩니다:

- 공식 `codex` 바이너리 — 대화를 이어 가기 위한 `codex queue`, 그리고 사용량을 읽고 자기가 넣어 둔 큐
  항목을 자기가 지우기 위한 `codex app-server --stdio`;
- 기대하는 인터페이스를 다루고 있는지 확인하기 위한 짧은 `codex --version`, `codex queue --help` 프로브;
- Codex 파일을 어느 프로세스가 점유하고 있는지 재시작 관리자에 묻고 알림을 띄우기 위한 Windows
  PowerShell.

모델 요청은 이미 로그인되어 있는 공식 Codex 바이너리가 수행하며, 사용자가 직접 그 메시지를 입력했을
때와 완전히 똑같습니다.

그 호출 하나하나에서 Codex 자신의 보고 기능도 함께 끕니다. 분석을 끄고, 모든 OpenTelemetry exporter를
none으로 두고, 프롬프트 로깅을 끄고, ChatGPT base URL을 공식 주소로 고정해 엉뚱한 로컬 설정이
continuation을 다른 곳으로 보낼 수 없게 합니다. 이는 이 도구가 시작하는 subprocess에 적용되는
설정이며, 사용자 자신의 Codex 세션에 적용되는 설정이 아닙니다. 그쪽은 아무 영향도 받지 않습니다.

## 무엇을 어디에 저장하는가

작업에 관해 기록하는 것은 전부 `%USERPROFILE%\.codex-auto-resume\` 아래에 있습니다:

- `config/state.sqlite` — 대기 중인 복구: 대화 UUID, 턴 번호, 타임스탬프, 장애 분류, 시도 횟수, 상태;
- `config/settings.json` — 사용자 설정;
- `logs/` — 워처가 무엇을 했는지. 상태 이름과 대화 UUID로 기록합니다.

로그는 내용이 아니라 상태 전이와 식별자를 기록합니다. 프롬프트 전문, 어시스턴트 출력, 도구 출력, 오류
본문은 로그에 쓰이지 않습니다.

그 디렉터리 바깥에는 평범한 사용자 단위 Windows 등록 항목만 남기며, 어느 것도 관리자 권한이 필요하지
않고 어느 것도 대화에 관한 내용을 담고 있지 않습니다. 시작 메뉴 바로 가기, 건너뛰지 않는 한 로그인 자동
시작 값, Windows가 토스트를 그리기 위해 반드시 요구하는 알림 발신자 식별자, 그리고 알림 버튼이 쓰는
`codex-auto-resume:` 링크의 핸들러입니다. 제거하면 모두 함께 사라집니다.

제거는 기본적으로 상태 디렉터리를 남겨 두므로 다시 설치해도 대기 중인 복구를 잃지 않습니다. purge 옵션을
준 `Uninstall.cmd`는 그것까지 지웁니다. 언제든 직접 지우셔도 됩니다.

## 알림

Windows 알림은 운영체제 자신의 알림 API를 통해 로컬에서 띄웁니다. 표시용 이름, 로컬 시각, 대화 UUID만
보여 주며 — 프롬프트 내용, 오류 문구, 계정 정보는 절대 보여 주지 않습니다. 어떤 서비스도 경유하지
않습니다.

## 제3자

**플러그인으로 설치하거나 업데이트할 때의 GitHub** — 위에서 설명한 다운로드가 전부이며, 그 밖에는
없습니다. 여느 다운로드와 마찬가지로 GitHub 자체의 개인정보 처리 방식이 적용됩니다.

그 밖의 제3자는 없습니다. 실행되는 것은 Python 표준 라이브러리, 릴리스 압축 파일에 함께 담겨 오는 인터프리터,
그리고 이 저장소에서 빌드한 작은 Windows 프로그램 두 개 — 설정 창과 MCP 런처입니다. 연동하는 대상은
이미 이 PC에 설치되어 있는 Codex뿐입니다.

## 향후 버전

옵트인 방식의 보고 기능이 언젠가 추가된다면, 기본값은 꺼짐이고, 무엇이든 보내기 전에 무엇을 보낼지
정확히 밝힐 것이며, 그 기능이 담긴 릴리스보다 이 문서가 먼저 그것을 설명할 것입니다. 오늘 그런 것은
존재하지 않습니다.

## 문의

이슈를 열어 주세요. 보안 신고 절차는 [SECURITY.md](SECURITY.md)를 참고하세요.
