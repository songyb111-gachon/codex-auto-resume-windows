# Feature matrix

Every capability this product claims, with the evidence level it has actually earned and
the citation that earns it. The third column is what makes a claim checkable; the fourth is
the point of the document, because a row whose strongest evidence is a unit test is not the
same product as a row somebody watched work.

## The vocabulary

These labels describe different kinds of evidence. PUBLISHED is a distribution status,
not a stronger test result. A published capability can still lack integration or visual
acceptance. Real Windows execution of an isolated component also does not establish a
real Codex recovery. Read each citation and its limits separately.

| Level | What it means |
| --- | --- |
| UNVERIFIED | None of the below. |
| IMPLEMENTED | The code exists and is reviewed. Nothing executes it in a check. |
| UNIT TESTED | A test drives the unit in isolation, usually with the things around it faked. |
| INTEGRATION TESTED | A test drives it through the real store, the real source and the real engine together. `tests/codexsim.py` builds a real Codex-shaped SQLite home - the tables and columns Codex itself writes - and every history and queue read goes through the production query layer, so a query that is wrong against the real schema fails there too. It is not a mock of the query layer. |
| REAL WINDOWS TESTED | It was run on a real Windows machine outside the ordinary logic of the suite: a compiled executable, a real named mutex, a real registry parser, a real PowerShell process, a captured screenshot. |
| REAL CODEX PROTOCOL TESTED | It was exercised against the real Codex desktop app's own databases or CLI, and the result is recorded under `docs/evidence/`. |
| REAL CODEX VISUALLY TESTED | A person watched it happen in Codex. |
| PUBLISHED | It is in a published release. |

## What this document is, and is not

- **It is written from the repository.** Every citation is something in this tree: a test
  module and test name, an artifact under `assets/` or `docs/images/`, a recorded
  observation under `docs/evidence/`, or a build output. Nothing here was established by
  reading a commit message or by remembering a session.
- **Nothing in it was verified by running the product against a real Codex conversation
  unless the row says so.** Five rows carry REAL CODEX PROTOCOL TESTED, and all five rest on
  the same 2026-09-06 measurements under `docs/evidence/`, taken against a disposable test
  thread. Everything else about recovery - correlation, outcomes, chains, budgets, expiry -
  has been driven only against `tests/codexsim.py`.
- **No row claims REAL CODEX VISUALLY TESTED.** Nothing in this repository records a person
  watching a recovery happen in Codex. The 2026-09-06 delivery check was confirmed through
  the desktop app's own task read and the thread's local history and queue, with
  `visual_ui_scraping_used: false` recorded in
  [`docs/evidence/unloaded-thread-observation.json`](evidence/unloaded-thread-observation.json).
  That is a protocol observation, not somebody watching.
- **This version, v0.6.5, is not released; v0.6.4 is the newest that is.** The rows below
  that say PUBLISHED mean a published version - v0.6.0, v0.6.1, v0.6.2, v0.6.3 or v0.6.4, all
  five of whose archives are published and whose digests are pinned on `main`. v0.6.3 added nine
  interface languages, settings for the continuation message, a popup on the
  notification-area icon, an Open Dashboard button on notifications and a redesigned window
  and panel, and fixed one defect present in the three versions before it: a recoverable
  kind of interruption that had no switch. The rows about those features still name what
  tested them; none of them says PUBLISHED, because none was re-run against the published
  bytes. v0.6.4 puts the Dashboard and the popup on the panel's materials in a light and a dark
  theme, makes the status light quieter, reopens the window by itself after a change of language
  or theme, gives the settings that pick items of a list a check box, pins the buttons and switches at
  the right of a card or row to its bottom-right corner, opens a faster window at 1000 × 632 whose
  Overview cards share the page's height and that scrolls on a soft bar, and fixes the combo boxes'
  clipped bottom edge; none of that says PUBLISHED either, for the same reason. v0.6.5 adds the Codex
  Compatibility Registry and its on-request refresh, the notification card, motion on the
  notification-area icon, a status light that blinks as the icon does, depth in the popup, drop-down lists
  and gliding switches drawn by the product, lists that fit their width, and five fixes (§17, and
  rows in §7 to §16). Until it is published nothing here claims PUBLISHED for any of
  that. What PUBLISHED does *not* mean anywhere here
  is that somebody installed it and used it: the published bytes were driven against a
  state directory and a Codex home that exist only for that check, with the installer's
  registrations never run and no watcher started against real Codex state. Installing,
  upgrading and uninstalling on a machine somebody uses is what `docs/LIVE_ACCEPTANCE.md`
  is for. That procedure was begun against v0.6.0, and its records are kept as history
  under `docs/evidence/live-0.6.0/`: one step passed and fourteen were blocked. Evidence
  belongs to one build, so none of it carries over, and `docs/evidence/live/` holds nothing
  for v0.6.5. A fresh v0.6.0 download matched the published sidecar and repository
  pin, passed exact-workflow/tag Sigstore verification, and matched all 107 installed
  payload files. Installed MCP, engine read paths, cooperative watcher stop/restart and
  update checking were exercised on Windows. None is a real Codex visual observation.
- **A REAL WINDOWS TESTED row usually means a test in the suite that needs a real Windows
  facility**, and those tests skip themselves where the facility is absent. A skipped test
  proves nothing, so the fourth column says where a skip is likely.

  For v0.6.5, the full run of this tree is TO FILL once the release candidate has run.

  For the record: on 2026-09-17, on the Windows 11 machine this document was written on,
  `PYTHONPATH="src;tests" python -m unittest discover -s tests` ran the v0.6.4 tree -
  **1820 tests, with no failures** - under Python 3.13 in 710 seconds with eight skipped,
  and under Python 3.12 in 715 seconds with seven skipped. On both, six of the skips were
  the opt-in live checks in `tests/test_integration_live.py`, each declining to run without
  `CODEX_AR_LIVE=1`, and one was `tests/test_convergence.py`'s check that a tagged current
  version is pinned, which skips until that version is tagged. The eighth, on 3.13 only, was
  `tests/test_workflow_privilege.py:YamlShapeTests.test_every_workflow_parses`, which wants
  PyYAML; that interpreter lacks it here, the test ran and passed under 3.12, and GitHub
  parses the workflow files on every push anyway. So every REAL WINDOWS TESTED row ran here,
  and none of the checks that would touch a real Codex installation did. The run includes the
  compiled window's reflection tests, the popup's real Windows window, the panel's scripts in
  Node, and `tests/test_screenshots.py` against the screenshots regenerated for v0.6.3 the
  same day; see §7. The opt-in live checks were not run against a real Codex environment for
  v0.6.3.

  A passing suite is not an acceptance. The first live acceptance of a published build
  found defects this suite did not, which is what v0.6.1 and v0.6.2 are; the changelog says
  which, and `docs/evidence/` keeps the records.

  An earlier draft of this document recorded a red run of 1140 tests, whose two failures were
  Korean-branch bookkeeping - an unmapped sibling and three stale translation digests - and
  neither touched the product. Both are fixed. One thing that draft did not catch is worth
  keeping: the run was green on the machine it was written on and red on CI, because two rows
  cited build outputs that are generated and git-ignored, present for anyone who had built the
  executables and absent in a fresh clone. `tests/test_feature_matrix.py` now asks whether the
  repository carries a cited path, not whether the machine does.

## How to read a citation

`tests/test_engine.py:GateTests.test_T47_a_subagent_or_non_desktop_thread_is_never_detected`
is module, then class, then test. Where several tests in one class back a row, the class is
named with a count rather than every method. Artifacts are given by path.

---

## 1. Detection and classification

| Capability | Evidence | What backs it | What is still missing |
| --- | --- | --- | --- |
| A usage limit is read from Codex's own turn row (`status=failed`, `codexErrorInfo=usageLimitExceeded`) | REAL CODEX PROTOCOL TESTED | `docs/evidence/usage-limit-sample.json`, captured read-only from a real Codex database on 2026-09-06. `tests/test_source.py:DetectionTests.test_real_usage_sample` runs detection over `tests/fixtures/usage-limit.json` - a sanitized copy of that capture, carrying its own made-up ids and its own field names - and not over the capture itself | One capture, one account, one Codex build. Nothing re-checks the shape against a newer Codex, and **nothing in the suite reads `docs/evidence/usage-limit-sample.json` at all**, so the fixture can drift away from the capture it was made from without a test noticing. |
| The reset time comes from the rate-limit snapshot preceding the failure, not from the message text | REAL CODEX PROTOCOL TESTED | The same capture records `resets_at` beside the UI's own "try again at" phrase, and `contains_machine_readable_reset_in_error: false`. `tests/test_source.py:DetectionTests.test_real_reset_and_timezone` chooses the reset from the fixture's own rate-limit rows and requires the fixture's own expected value; `test_low_usage_hint_is_not_adopted_as_reset` works from values written in the test | Nothing checks the two still agree on a current Codex, and nothing re-checks the fixture against the published capture. |
| A failed turn's rollout event is named `task_complete` and still carries the error, so an event name is never read as success | REAL CODEX PROTOCOL TESTED | `docs/evidence/usage-limit-sample.json` (`rollout_failure.payload_type: task_complete`); `tests/test_source.py:LocalSourceTests.test_detect_actual_shape` detects that failure in a hand-built Codex home seeded from the fixture's copy of the rollout event, not from the capture | The same single capture, and nothing re-checks the fixture against it. |
| Transient failures are classified and recovered on their own policy | INTEGRATION TESTED | `tests/test_failures.py:TransientTests` (3 tests); `tests/test_recovery.py:PolicySeparationTests.test_a_transient_failure_waits_on_backoff_not_on_a_reset` | No recorded observation of a real transient Codex failure; the codes come from the schema, not from a capture. |
| Terminal failures are never retried | UNIT TESTED | `tests/test_failures.py:TerminalTests` (3 tests, incl. `test_authentication_is_terminal_not_a_retry_budget`); `tests/test_recovery.py:UnknownNeverRetriedTests.test_a_terminal_failure_is_never_registered` | No captured real instance of any terminal code. |
| An unclassified failure is never registered and never retried | INTEGRATION TESTED | `tests/test_failures.py:UnknownTests` (6 tests), `DetectionGateTests.test_only_recoverable_categories_are_ever_registered`; `tests/test_recovery.py:UnknownNeverRetriedTests.test_an_unclassified_failure_is_never_registered` | - |
| A rate limit Codex itself gave up on (`429`, too many attempts) waits at least a minute | INTEGRATION TESTED | `tests/test_outcomes.py:T27RateLimitTests` (3 tests); `tests/test_recovery.py:PolicySeparationTests.test_T27_a_rate_limit_codex_gave_up_on_waits_at_least_a_minute` | Not observed against a real 429. |
| Only the exact conversation UUID is ever acted on; `--last` is never used | INTEGRATION TESTED | `tests/test_engine.py:EngineScenarioTests.test_18_one_thread_failure_never_resumes_another`; `tests/test_windows.py:BackendTests.test_queue_argv_exact_id_shell_disabled_and_no_telemetry`, `test_invalid_thread_id_never_launches` | - |
| Subagent, archived and non-desktop conversations are never detected | INTEGRATION TESTED | `tests/test_source.py:ChildThreadTests` (3 tests, incl. `test_T47_state_row_and_rollout_must_both_say_user`); `tests/test_engine.py:GateTests.test_T47_a_subagent_or_non_desktop_thread_is_never_detected` | The originator values come from the simulated home; no capture of a real subagent thread. |
| A failure older than the look-back is never detected later | INTEGRATION TESTED | `tests/test_engine.py:EngineScenarioTests.test_12b_disabled_at_failure_time_is_not_detected_later_beyond_lookback`; `tests/test_source.py:LocalSourceTests.test_ignore_historic_failure` | - |
| Detection keeps no error text | UNIT TESTED | `tests/test_failures.py:DetectionGateTests.test_no_error_text_survives_detection` | - |
| Every recoverable kind of interruption has a name and a switch, and the list of switches and the classifier agree in both directions | UNIT TESTED | `tests/test_reasons.py:RegistryAgreementTests` (5 tests, incl. `test_every_recoverable_category_has_a_switch_and_every_switch_a_category`, `test_no_recoverable_category_is_recovered_without_a_switch`), `EntryTests` (4 tests, incl. `test_an_unheard_of_category_is_named_as_unknown_and_is_not_recoverable`) | Two lists in the source held to each other; nothing here involves a real failure. It exists because through v0.6.2 `auth_service_transient` was recovered with no switch to turn it off and no name in any window. |

## 2. The gates

Every send passes a fixed vector of gates. A gate that refuses is recorded beside the
record rather than folded into its state.

| Capability | Evidence | What backs it | What is still missing |
| --- | --- | --- | --- |
| The gate vector is written on a granted claim, and a refusal makes no transition | INTEGRATION TESTED | `tests/test_outcomes.py:T31GateEvalTests.test_T31_the_gate_vector_is_written_on_a_granted_claim`, `test_T31_a_refused_claim_writes_consent_block_and_makes_no_transition` | - |
| Global pause is an immediate kill switch and preserves what is pending | INTEGRATION TESTED | `tests/test_engine.py:EngineScenarioTests.test_12_global_disable_is_immediate_kill_switch`; `tests/test_control.py:StatusTests.test_pause_preserves_pending_records` | - |
| A conversation switched off never queues, and others are unaffected | INTEGRATION TESTED | `tests/test_engine.py:EngineScenarioTests.test_13_thread_disabled_never_queues_but_other_thread_does`; `tests/test_outcomes.py:T31GateEvalTests.test_T31_a_due_record_on_a_disabled_thread_records_consent_block` | - |
| Nothing is sent before the real reset time | INTEGRATION TESTED | `tests/test_engine.py:EngineScenarioTests.test_07_before_reset_never_queues`, `GateTests.test_T31_a_future_reset_is_a_wait_with_a_reason` | - |
| Nothing is sent while the desktop app is closed, the conversation is not loaded, or loaded state is unknown | INTEGRATION TESTED | `tests/test_engine.py:EngineScenarioTests.test_09_after_reset_not_loaded_waits`, `test_11_app_closed_never_queues`, `test_unknown_loaded_state_never_queues` | The gate exists because of the finding below; the gate itself has never run against a real unloaded thread. |
| A message queued for an unloaded conversation is not delivered as a turn - the finding the gate is built on | REAL CODEX PROTOCOL TESTED | `docs/evidence/unloaded-thread-delivery.json` (verdict FAIL after a genuine app restart, 90-second window) and `docs/evidence/unloaded-thread-observation.json` | One 90-second window, one Codex build. Codex may deliver later; the evidence says so and the README repeats it. |
| A message queued for a loaded conversation is delivered into that exact conversation | REAL CODEX PROTOCOL TESTED | `docs/evidence/loaded-thread-delivery.json` (`same_thread_user_message_confirmed`, `same_thread_agent_message_confirmed`, `pending_test_items_after_delivery: 0`) | Confirmed through the app's task read and local history, not watched by a person. One thread, one run, 2026-09-06. |
| Loaded state is decided from the Restart Manager, and this tool never takes the app's writer lock | UNIT TESTED | `tests/test_windows.py:BackendTests`, eight loaded-state tests in that class (incl. `test_tool_never_acquires_the_apps_writer_lock`, `test_pid_reuse_fails_closed`, `test_ambiguous_resource_users_fail_closed`) | The classifier runs against faked Restart Manager output. `tests/test_integration_live.py:LiveReadOnlyTests.test_loaded_state_classification_of_existing_locks` would check it for real; it is one of the six tests skipped without `CODEX_AR_LIVE=1`, and nothing here records a run. |
| Live usage is re-checked immediately before a send, and an unavailable probe waits | INTEGRATION TESTED | `tests/test_engine.py:EngineScenarioTests.test_15d_usage_still_unavailable_does_not_queue`; `tests/test_windows.py:UsageTests` (9 tests, incl. `test_missing_or_malformed_safe_unknown`, `test_private_response_fields_are_discarded`) | The real probe is exercised only by the opt-in live check, which nothing records running. |
| A history projection that has fallen behind its rollout blocks the send | INTEGRATION TESTED | `tests/test_outcomes.py:T32ProjectionTests` (5 tests); `tests/test_source.py:LocalSourceTests.test_projection_fresh_when_caught_up`, `test_projection_not_fresh_when_the_rollout_grew` | The staleness thresholds were chosen, not measured against a real Codex under load. |
| A missing projection table blocks as incompatible rather than passing | INTEGRATION TESTED | `tests/test_outcomes.py:T32ProjectionTests.test_T32_a_missing_projection_table_blocks_as_incompatible`; `tests/test_engine.py:GateTests.test_T32_a_missing_projection_table_blocks_compatibility` | - |
| One send in flight per conversation; a cooldown and a daily cap | INTEGRATION TESTED | `tests/test_engine.py:EngineScenarioTests.test_thread_cooldown_and_daily_cap`, `test_15e_a_launch_retry_still_respects_the_thread_cooldown` | - |
| Two engines on one state send once | INTEGRATION TESTED | `tests/test_engine.py:EngineScenarioTests.test_16_two_engines_on_same_state_send_once` | Two engines in one process against one file - not two real watcher processes racing. |
| The same interruption is never resumed twice, across a crash or a restart | INTEGRATION TESTED | `tests/test_engine.py:EngineScenarioTests.test_crash_after_reserve_before_send_never_resends`, `test_15b_unknown_send_outcome_is_never_resent`, `test_14_watcher_restart_preserves_pending`; `tests/test_store.py:StoreTests.test_reserved_interruption_survives_crash_as_submitting`, `test_unknown_submission_can_resolve_but_never_retry` | - |
| A policy setting cannot reach a safety gate | UNIT TESTED | `tests/test_engine.py:PolicyTests.test_policy_cannot_reach_a_safety_gate`, `test_no_setting_names_an_engine_safety_option`; `tests/test_settings.py:DefaultsTests.test_no_setting_can_enable_recovery_of_an_unknown_failure` | - |

## 3. Exact turn correlation

| Capability | Evidence | What backs it | What is still missing |
| --- | --- | --- | --- |
| The continuation carries a marker built from the interruption's own id, and the recovery turn is the marker row's turn - not the latest turn | INTEGRATION TESTED | `tests/test_correlation.py:CorrelationTest.test_T01_recovery_turn_is_the_marker_rows_turn_even_after_a_later_user_turn`; `tests/test_engine.py:CorrelationTests.test_T01_the_recovery_turn_survives_a_watcher_restart`; `tests/test_source.py:LocalSourceTests.test_marker_rows_report_their_own_turn_not_the_latest` | Never correlated against a marker a real Codex wrote. The marker rows in the simulation are written by the simulation. |
| The continuation's words follow the settings - Minimal, Standard (the default), Detailed or Custom, in a continuation language that follows the interface unless one is chosen - are decided before anything is claimed, and still end in the marker | INTEGRATION TESTED | `tests/test_continuation.py:WatcherSendsTheConfiguredMessageTests` (6 tests, incl. `test_the_text_is_decided_before_anything_is_claimed`, `test_a_change_made_while_it_waits_applies_to_that_recovery`, `test_a_failure_building_the_text_falls_back_and_is_logged`) drive the engine against `tests/codexsim.py`; `EveryReasonEveryLanguageTests` (4 tests) build every style for every recoverable kind in every language, and `SettingsResolutionTests` (3 tests) cover the defaults and a stored value this version does not know | No continuation in any of the new styles or languages has been sent into a real Codex conversation. **This changed a default:** before v0.6.3 the text was one fixed English sentence per kind of interruption; it is now the Standard message in the continuation language. |
| A Custom message is sent exactly as written - never translated - may use only `{reason}`, `{category}`, `{attempt}`, `{max_attempts}` and `{reset_time}`, is at most 2000 characters, and falls back from the message for one kind of interruption to the message for every interruption, then to Standard | UNIT TESTED | `tests/test_continuation.py:CustomMessageTests` (15 tests, incl. `test_forbidden_and_unknown_placeholders_are_refused_by_name`, `test_length_and_emptiness`, `test_the_fallback_order_is_per_reason_then_global_then_standard`, `test_custom_text_can_never_be_attached_to_a_failure_that_is_not_recovered`), `NoLeakageTests.test_nothing_from_the_record_but_the_safe_fields_reaches_the_text` | What the words say is the person's: the placeholders are limited and the rest of the text is not, and nothing checks that a Custom message helps Codex carry on. |
| Preview is the text the watcher would send, built by the same function, for each kind of interruption and for choices not yet saved, and it saves nothing | UNIT TESTED | `tests/test_control_continuation.py:PreviewTests` (10 tests, incl. `test_the_preview_is_the_text_the_watcher_builds`, `test_unsaved_choices_are_previewed_and_nothing_is_saved`, `test_a_refused_custom_message_shows_what_would_really_be_sent`); `BridgeCommandTests.test_the_window_reaches_all_three` reaches it through the bridge | The Preview leaves out the marker line the watcher adds after the text, and says so. Neither the Dashboard's Preview nor the panel's is driven the way a person uses it. |
| The engine has no way to ask for "the latest turn" | UNIT TESTED | `tests/test_source.py:StaticQueryTests.test_T01_latest_turn_id_is_gone`; `tests/test_engine.py:CorrelationTests.test_T01_latest_turn_id_is_gone_from_the_source`. Both read the files under `src/` and require the identifier `latest_turn_id` to be absent; neither runs a query | What is checked is that one spelling is gone, not that the capability is. A renamed helper that still returned the latest turn would pass both. |
| Two marker rows are never correlated; the record stays ambiguous and our queued row is still taken back | INTEGRATION TESTED | `tests/test_correlation.py:CorrelationTest.test_T02a_two_marker_rows_are_never_correlated`, `test_T02a_two_marker_rows_still_withdraw_our_queued_row` | - |
| A marker with a different client id, or at or before the failed turn, is ambiguous | INTEGRATION TESTED | `tests/test_correlation.py:CorrelationTest.test_T02c_different_client_id_is_an_ambiguous_receipt`, `test_T02e_marker_at_or_below_the_failed_ordinal_is_ambiguous`, `test_T02d_null_client_ids_leave_the_marker_rules_alone` | The client id column's real values come from one Codex build. |
| A marker steered into somebody else's turn is handed over, not claimed | INTEGRATION TESTED | `tests/test_correlation.py:CorrelationTest.test_T02b_marker_steered_into_someone_elses_turn_is_handed_over` | - |
| One Codex turn can belong to at most one record | UNIT TESTED | `tests/test_store.py:TransitionTableTests.test_T12_a_recovery_turn_is_written_once` (the unique index) | - |
| An undetermined turn is never a reason to delete a queued row, and never holds back a cancel or a pause | INTEGRATION TESTED | `tests/test_correlation.py:WatchTest.test_T03_undetermined_turn_is_never_a_reason_to_delete`, `test_T03_an_undetermined_turn_never_holds_back_a_cancel_or_a_pause` | - |
| A change between the claim and the send releases the claim, and nothing is announced in between | INTEGRATION TESTED | `tests/test_correlation.py:WatchTest.test_T08_change_between_reserve_and_send_releases_the_claim`, `test_T08_no_notification_between_claim_and_send`; `tests/test_outcomes.py:T48NotificationTests.test_T48_nothing_is_announced_between_the_claim_and_the_send` | - |
| A foreign item queued before or behind ours makes us wait, or take ours back | INTEGRATION TESTED | `tests/test_correlation.py:WatchTest.test_T09_foreign_queued_input_makes_the_record_wait`, `test_T09_foreign_item_queued_while_ours_is_queued_withdraws_ours` | - |
| An edited queued item is handed over without a delete | INTEGRATION TESTED | `tests/test_correlation.py:WatchTest.test_T10_edited_queued_item_is_handed_over_without_a_delete` | Editing is simulated by rewriting the queue row; nobody has edited a queued item in Codex and watched the result. |
| A marker already in Codex releases the claim as a duplicate owner | INTEGRATION TESTED | `tests/test_engine.py:CorrelationTests.test_T45_a_marker_already_in_codex_releases_the_claim_as_a_duplicate_owner` | - |

## 4. Outcomes

| Capability | Evidence | What backs it | What is still missing |
| --- | --- | --- | --- |
| The outcome is read from the correlated turn and no other | INTEGRATION TESTED | `tests/test_outcomes.py:T13OutcomeRulesTests` (6 tests); `tests/test_engine.py:OutcomeTests` (11 tests) | - |
| A turn that never finishes is unverified at the deadline - never a success | INTEGRATION TESTED | `tests/test_outcomes.py:T13OutcomeRulesTests.test_T13_a_row_that_stays_in_progress_becomes_unverified_at_the_deadline`, `test_T13_a_later_terminal_turn_proves_an_in_progress_row_stale` | - |
| An unreadable history is retried to the deadline and never becomes "no progress" | INTEGRATION TESTED | `tests/test_outcomes.py:T13OutcomeRulesTests.test_T13_unreadable_history_is_retried_then_hits_the_deadline_never_no_progress`, `test_T13_history_that_becomes_readable_again_is_evaluated_normally` | - |
| An interrupted recovery turn is "stopped by the user" | INTEGRATION TESTED | `tests/test_outcomes.py:T13OutcomeRulesTests.test_T13_an_interrupted_recovery_turn_is_stopped_by_the_user` | The status string comes from the schema; no capture of a real user interrupt. |
| A completed turn that produced nothing is "no progress", in a chain as well as alone | INTEGRATION TESTED | `tests/test_outcomes.py:T15NoProgressTests` (2 tests) | - |
| A person joining the turn hands it over at once, and its child is superseded rather than claimed | INTEGRATION TESTED | `tests/test_outcomes.py:T16UserJoinedTests` (3 tests) | - |
| Nothing is judged before the turn's own items are projected, and a terminal status first seen late still waits one more tick | INTEGRATION TESTED | `tests/test_outcomes.py:T14SettleTests` (2 tests); `tests/test_engine.py:OutcomeTests.test_T14_a_turn_first_seen_long_after_it_finished_still_waits_one_tick` | - |
| Progress is decided without reading any content | INTEGRATION TESTED | `tests/test_recovery.py:NoProgressTests.test_progress_is_decided_without_reading_any_content`; `tests/test_source.py:LocalSourceTests.test_turn_observation_progress_only_known_types_in_that_turn` | - |
| A stale observing record does not block a newer failure | INTEGRATION TESTED | `tests/test_outcomes.py:T17StuckObservingTests` (2 tests) | - |
| A late delivery after an unknown result is announced | INTEGRATION TESTED | `tests/test_outcomes.py:T48NotificationTests.test_T48_a_late_delivery_after_an_unknown_result_is_announced`; `tests/test_engine.py:OutcomeTests.test_T48_a_late_delivery_after_an_unknown_result_is_announced` | - |

## 5. Budgets, chains and usage expiry

| Capability | Evidence | What backs it | What is still missing |
| --- | --- | --- | --- |
| A failure of our own recovery turn continues its parent's chain rather than starting a new one | INTEGRATION TESTED | `tests/test_outcomes.py:T18ChainTests` (5 tests); `tests/test_recovery.py:ChainTests` (6 tests) | - |
| The child inherits every counter in the one transaction that creates it | UNIT TESTED | `tests/test_store.py:CrashInjectionTests.test_T19_register_with_a_parent_is_all_or_nothing`; `tests/test_store.py:StoreTests.test_every_record_belongs_to_a_chain` | - |
| A cancelled, handed-over or exhausted parent makes its child arrive already stopped | INTEGRATION TESTED | `tests/test_outcomes.py:T20LineageTests` (2 tests); `tests/test_recovery.py:ChainTests.test_T20_a_cancelled_parent_stops_its_child_before_its_own_outcome`; `tests/test_store.py:CancelTests.test_T21_nothing_running_reports_no_change_but_blocks_a_future_child` | - |
| Six continuations per task by default; the setting takes 1 to 10 and nothing outside | UNIT TESTED | `tests/test_engine.py:PolicyTests.test_the_chain_cap_can_be_lowered_but_never_raised_past_ten`, `test_the_attempt_budget_comes_from_the_settings`; `tests/test_settings.py:DescribeTests.test_published_ranges_accept_their_own_bounds` | - |
| The attempt budget holds across records and across a restart | INTEGRATION TESTED | `tests/test_recovery.py:BudgetTests` (3 tests, incl. `test_attempts_persist_across_a_restart`) | - |
| A usage chain stops at the continuation cap whatever the attempt budget | INTEGRATION TESTED | `tests/test_outcomes.py:T18ChainTests.test_T18_a_usage_chain_stops_at_the_chain_cap_whatever_the_attempt_budget` | - |
| Time without usage accumulates across a chain and expires it at seven days | INTEGRATION TESTED | `tests/test_outcomes.py:T28UsageExpiryTests` (4 tests); `tests/test_engine.py:GateTests.test_T28_the_never_available_limit_is_seven_days`, `test_T28_only_unavailable_time_between_probes_accumulates` | Seven days of real wall-clock behaviour has never been observed; the clock is the test's. |
| A weekly limit never expires before its own reset plus a day | INTEGRATION TESTED | `tests/test_outcomes.py:T28UsageExpiryTests.test_T28_a_weekly_reset_never_expires_before_reset_plus_a_day`; `tests/test_engine.py:GateTests.test_T28_a_weekly_limit_never_expires_before_its_reset` | - |
| Giving attempts back is bounded to three per task, belongs to the chain, restores only the two exhausted states, and sends nothing | UNIT TESTED | `tests/test_store.py:RestoreBudgetTests` (13 tests, incl. `test_T26_the_fourth_reset_of_a_record_is_refused`, `test_T26_the_reset_count_belongs_to_the_chain`, `test_does_not_submit`, `test_never_reactivates_something_that_may_have_been_sent`) | - |
| It re-enters the wait its kind of failure needs, and leaves a switched-off conversation off | UNIT TESTED | `tests/test_store.py:RestoreBudgetTests.test_T26_usage_record_returns_to_its_reset_wait_and_transient_to_backoff`; `tests/test_control.py:BudgetTests.test_reset_returns_a_record_to_the_wait_its_kind_of_failure_needs`, `test_reset_leaves_a_switched_off_conversation_off` | - |
| Retry now moves only the schedule: it does not send, does not skip a gate, does not open a usage window | UNIT TESTED | `tests/test_store.py:RetryNowTests` (3 tests); `tests/test_control.py:RetryNowTests` (4 tests); `tests/test_control_v3.py:RetryNowTests` (2 tests); `tests/test_mcp.py:ToolBehaviourTests.test_retry_now_does_not_submit`, `test_retry_now_says_the_checks_still_apply` | - |
| Retry now wakes the watcher | REAL WINDOWS TESTED | `tests/test_surface_properties.py:WakeEventTests` (4 tests) hold a real named event in a second process waiting two minutes on it, signal it from this one, and require the wait to end on that event in well under thirty seconds; also that signalling with nobody waiting answers false, that one state directory's event does not reach another's, and that the fallback with no event still sleeps rather than spinning; the control layer reports whether anything woke, and `tests/test_control_v3.py:RetryNowTests` covers the "no watcher to wake" answer | The waiter is a test process rather than a real watcher, so what is shown is that the wait ends - not that the tick it then runs re-evaluates everything, which is `tests/test_engine.py`'s business. |
| Cancel reaches every record of the task and no other, and is retried under a write lock rather than refused | UNIT TESTED | `tests/test_store.py:CancelTests` (3 tests); `tests/test_control_v3.py:ChainCancelTests` (2 tests); `tests/test_outcomes.py:T23CancelContentionTests` (2 tests, incl. `test_T23_cancel_succeeds_by_retrying_under_a_12_second_write_lock`) | - |
| Cancel all cancels every pending recovery, each by its own interruption id, turns no conversation off, and has no bulk retry beside it | UNIT TESTED, at the control layer | `tests/test_control_continuation.py:CancelAllTests` (4 tests, incl. `test_every_pending_recovery_is_cancelled_by_its_own_id`, `test_it_never_turns_a_conversation_off`, `test_the_layer_offers_no_bulk_retry`); `BridgeCommandTests.test_the_window_reaches_all_three` | The Dashboard's button and its confirmation are not driven. |
| Cancel racing the send marks the record and the watch withdraws it | INTEGRATION TESTED | `tests/test_outcomes.py:T22CancelRaceTests` (2 tests); `tests/test_engine.py:PauseAndCancelTests.test_T22_cancel_racing_the_send_marks_and_the_watch_withdraws` | That a turn already running in Codex is not stopped is documented, not observed against Codex. |
| Pause withdraws a queued continuation and returns the record to waiting; a withdrawal over a stale history is never released | INTEGRATION TESTED | `tests/test_outcomes.py:T24PauseTests` (5 tests), `T25PausedWatchTests` (4 tests); `tests/test_pause_unknown.py:PausedUnknownTests.test_a_pause_withdraws_it_and_it_ends_final` | The withdrawal is the simulated App Server delete; a real withdrawal has never been measured. |

## 6. The public vocabulary

| Capability | Evidence | What backs it | What is still missing |
| --- | --- | --- | --- |
| 27 stored states map onto 22 public codes, exactly as the design table says | UNIT TESTED | `tests/test_outcomes.py:T29T30PublicCodeTests.test_T29_there_are_twenty_seven_stored_states`, `test_T29_T30_every_stored_record_maps_as_the_design_table_says`, `test_T29_every_stored_failed_is_failed_terminal`; `tests/test_labels_v3.py:LabelTests.test_every_stored_state_maps_to_a_public_code` | - |
| A public code never depends on a setting | UNIT TESTED | `tests/test_outcomes.py:T29T30PublicCodeTests.test_T29_the_code_does_not_change_with_the_attempt_budget`, `test_T29_the_mapping_module_imports_no_settings`; `tests/test_labels_v3.py:LabelTests.test_a_failed_record_is_never_described_as_retrying` | - |
| Overlays never apply to a record that may already have been sent | UNIT TESTED | `tests/test_outcomes.py:T29T30PublicCodeTests.test_T30_overlays_never_apply_to_a_record_that_may_have_been_sent`, `test_T30_a_claimed_record_with_a_receipt_or_queue_id_reads_as_submitted` | - |
| Every public code and overlay has a label in every language | UNIT TESTED | `tests/test_labels_v3.py:LabelTests.test_every_public_code_and_overlay_has_a_label_in_every_language`; `tests/test_locale.py:CatalogTests` (9 tests) | Nobody has recorded reading the labels in any of the eight translations in the running window. The committed screenshots show them in English and Korean, and on the Overview and Pending pages in Japanese, Simplified Chinese and German, drawn from sample records; a picture of a label is not somebody reading it. |
| Nine interface languages - English, Korean, Japanese, Simplified Chinese, Traditional Chinese, Spanish, German, French and Brazilian Portuguese - each a JSON catalog, with English the source and the fallback for any key a translation has not reached | UNIT TESTED | `tests/test_l10n.py:ShippedCatalogTests` (7 tests), `LoaderTests` (5 tests, incl. `test_a_missing_key_is_english_and_a_present_one_is_translated`, `test_a_broken_translation_falls_back_to_english_instead_of_failing`), `NoNetworkTests.test_localization_reaches_no_network`; `tests/test_locale.py:CatalogTests` (9 tests), `ReachTests.test_one_language_reaches_all_four` | No native speaker of any of the eight translations has reviewed them. Completeness, placeholders and currency are checked; wording is not. |
| A translation made from English that has since changed is reported stale until it is looked at again, and an import that loses a placeholder or adds an unknown key is refused whole | UNIT TESTED | `tests/test_l10n.py:ToolTests` (4 tests), `ShippedCatalogTests.test_every_translation_is_complete_and_current` | The tool lives under `build/`, which the release archive leaves out. |
| Interface language: System, the default, follows the first language Windows lists; a language this product does not ship is English; an explicit choice wins over Windows; `CODEX_AUTO_RESUME_LANG` still overrides both | UNIT TESTED | `tests/test_l10n.py:PreferenceTests` (4 tests), `NormalizationTests` (3 tests); `tests/test_locale.py:DecisionTests` (8 tests, incl. `test_the_explicit_override_wins_in_both_directions`); `tests/test_control_continuation.py:BridgeCommandTests.test_strings_follow_the_stored_interface_language` | Windows' preference list is supplied by the tests. Nothing in this repository records a real change of Windows' display language, or a choice surviving a real restart, repair or update. |
| The window, the panel and the tools show the public code; the command line also prints the stored state | UNIT TESTED | `tests/test_control_v3.py:HistoryAndStatisticsTests.test_the_timeline_code_is_the_code_the_listing_shows`, `test_listing_carries_the_public_code_and_overlays` | - |
| Every refusal carries a code from a closed set, the same code every time, beside a sentence | UNIT TESTED | `tests/test_control.py:ErrorCodeTests` (11 tests); `tests/test_mcp.py:RefusalTests` (6 tests); `tests/test_control_v3.py:RefusalCodeTests` (5 tests); `tests/test_locale.py:CatalogTests.test_every_refusal_code_has_a_sentence_in_every_language` | - |

## 7. The Dashboard

The window is C#. Most of it is checked by asserting the shape of `gui/Dashboard.cs`, or by
testing the control layer it calls. Only a few rows run the compiled program.

| Capability | Evidence | What backs it | What is still missing |
| --- | --- | --- | --- |
| Six pages - Overview, Pending, History, Statistics, Diagnostics, Settings | REAL WINDOWS TESTED | `gui/Dashboard.cs` builds all six, and all six are captured from the compiled window with `build/capture_window.ps1` at 144 DPI in English and Korean - `docs/images/dashboard-overview.png`, `dashboard-pending.png`, `dashboard-history.png`, `dashboard-statistics.png`, `dashboard-diagnostics.png` and `settings-window.png` - and the Overview, Pending and Settings pages in Japanese, Simplified Chinese and German (`dashboard-overview-ja.png` and so on), pinned in `assets/screenshots.json` and checked by `tests/test_screenshots.py:ManifestTests` (5 tests) | Each is one page at one size with one set of synthetic records. A picture shows a page rendering; it does not show a person using it. The pictures are made again whenever what the window is drawn from changes (next row), so they show the window its current sources draw, from sample data. Hover, keyboard focus, High Contrast and the status light's motion are in none of them; the Settings picture is the Continuation message section. |
| The screenshots are current: the suite fails when what they were rendered from changes | UNIT TESTED | `tests/test_screenshots.py:ManifestTests.test_every_render_input_is_unchanged_since_the_images_were_made`, `test_the_committed_images_are_the_ones_the_manifest_describes`, `test_the_window_inputs_include_what_the_dashboard_is_computed_by`, `test_the_recorded_version_is_the_current_version`; `ContentTests.test_the_sample_version_comes_from_the_manifest` makes the version the pictures show follow the plugin manifest; `ContentTests.test_every_surface_is_drawn_in_the_pinned_theme` holds the window, the popup and the panel pictures to the light theme, whatever theme the machine that makes them uses | The manifest records the version the images were made for, and the suite fails unless it is the current one. A current picture is still a picture of sample data at one size. **Only the notification card is pictured in the dark theme**; no committed picture shows the window, the popup or the panel in dark. |
| The sample data in the screenshots carries no real identifier | UNIT TESTED | `tests/test_screenshots.py:ContentTests.test_the_window_sample_carries_no_real_identifier`, `test_the_sample_data_carries_no_real_identifier` | - |
| The icon's motion is pictured in the README as a GIF drawn from the icon's own frames - watching, recovering, needing attention and paused, at 48 px with the badge each wears, on a light and a dark taskbar - at the moments the icon's own timer shows them; the GIF is pinned under `<icon motion>`, which moves when the motion, its numbers, the mark or its colours change and not when the icon's menu, the popup or the card does, nor when the motion moves to another module; it is small, every picture in it is held at least two hundredths of a second, which is the least a browser shows one for, and the generator writes it byte for byte | UNIT TESTED, as a picture drawn from the frames | `docs/images/icon-motion.gif` in `assets/screenshots.json`; `tests/test_screenshots.py:IconMotionPictureTests` (8 tests), among them `test_no_picture_is_shorter_than_a_browser_shows_it`, which reads the delays out of the committed GIF | Drawn, not recorded: it is not the notification area itself. That browsers stretch a delay of 10 ms or less to 100 ms was measured in headless Edge while this was built, and is recorded nowhere here. A GIF holds 256 colours, so the frames share 255; about 2% of the pixels are off the icon's own, by up to 29 in a channel. |
| The notification pictured is the card the product shows: the notice the watcher's own builder makes for a usage limit on the sample's conversation, in the catalogs' words, drawn off-screen by the card's own code in the light and the dark theme in English and Korean and in the light theme in Japanese, Simplified Chinese and German; each picture is pinned under `<card render:*>`, which moves when what the card says or the code that draws it changes, and not when that code moves or the machine differs | REAL WINDOWS TESTED, as pictures rendered off-screen | `docs/images/notification-card.png`, `docs/images/notification-card-dark.png` and their ko, ja, zh-CN and de siblings, in `assets/screenshots.json`; `tests/test_screenshots.py:CardPictureTests` (7 tests) | The card at rest over a plain ground, at one scale: not its entrance, its stack, a real wallpaper or the taskbar beside it. Windows' own notification, which appears instead wherever a card must not, is not pictured. |
| The window talks to one long-lived bridge process, one JSON line per request | UNIT TESTED | `tests/test_gui_layout.py:PersistentBridgeTests.test_the_serve_command_line_answers_a_request` runs the exact command line the window builds and requires a real reply; `tests/test_control.py:BridgeTests.test_serve_answers_every_line_with_exactly_one_line`, `test_serve_framing_edge_cases_each_get_the_answer_they_should`, `test_serve_cannot_reach_anything_the_one_shot_form_cannot` | The compiled window's own client of that bridge is never driven. |
| The whole Overview arrives in one round trip, each part failing on its own | UNIT TESTED | `tests/test_control.py:BridgeTests.test_a_dashboard_part_that_fails_costs_only_that_part` | Tested at the bridge. Whether the window renders a failed part correctly is not checked. |
| The bridge answers `status` without importing the watcher, and its probe of the watcher's mutex gives the answer the watcher's own gives - not running, running or unknown | REAL WINDOWS TESTED | `tests/test_control.py:BridgeImportTests` (2 tests) run the real bridge, one-shot and serving, in a subprocess against an empty Codex home, and require it to have probed and to have loaded none of `app`, `notify`, `tray_popup` and `xml.sax`; `WatcherProbeTests` (3 tests) ask both probes while the mutex is free, while another process holds it and once that process is gone, and with the mutex call made to fail; `tests/test_notify.py:ImportCostTests.test_importing_the_module_does_not_load_xml_sax`, and `ToastPayloadTests.test_the_escaping_itself_has_not_moved` holds a toast's escaping to what it was before that import moved | Skipped off Windows. The bridge's probe is now a copy of the watcher's, and these tests are all that keeps the two in step; the unknown answer comes from patching the mutex call, not from a squatted object. The status check's 341 ms to 168 ms in the changelog was measured during development against an empty state directory, and no test measures time. |
| A part that cannot be read is shown as unreadable, not as an empty list | REAL WINDOWS TESTED | `tests/test_gui_decisions.py:DecisionTests` (3 of 11 tests) call the compiled window's own `Unreadable` with a snapshot where one part came back as an error, the other, both and neither, and its own `ShowUnreadable` against a real `ListView` already holding a row and a real `Label` already saying "Nothing is waiting" - which must end up emptied, visible, and saying it cannot be read; the bridge half is `tests/test_control.py:BridgeTests.test_a_dashboard_part_that_fails_costs_only_that_part` | **No picture of it.** The decision and the labels are driven, and the window has never been photographed in that state, which is what `docs/LIVE_ACCEPTANCE.md` step 12 is for. |
| Nothing that talks to the bridge runs on the UI thread | UNIT TESTED | `tests/test_gui_layout.py:WatcherStartReportingTests.test_the_wait_does_not_happen_on_the_ui_thread`, `test_it_does_not_wait_a_fixed_time_and_hope`, `test_it_reads_the_state_the_engine_reported` | These are source-shape assertions: they pin the reasoning, and pass on code that is wrong in a new way. |
| Each action addresses a recovery by its exact interruption id, and each confirmation names the conversation | UNIT TESTED, at the control layer | `tests/test_control.py:IdentifierTests` (3 tests); `tests/test_control_v3.py:ThreadSwitchTests.test_the_switch_needs_an_exact_id` | The C# half - that the id sent is the selected row's, and that the confirmation really carries the name - is unverified. |
| Retry now is offered only where it can do something; giving attempts back is offered where it cannot send | REAL WINDOWS TESTED | `tests/test_gui_decisions.py:DecisionTests` (4 of 11 tests) load the compiled window and call its own `CanRetryNow` and `CanGiveAttemptsBack` against eighteen records - a reset passed and a reset still ahead, paused, switched off for the conversation, cancelled, out of attempts with and without resets left, a code nothing knows, and no record at all - and against a busy window; every refusal those conditions avoid is tested at the control layer: `tests/test_control_v3.py:RefusalCodeTests.test_each_retry_now_refusal_arrives_as_its_own_code`, `test_each_restore_budget_refusal_arrives_as_its_own_code` | The decision is checked; that the window wires it to the right button is still read rather than run. |
| The timeline shows one chain in the public words and is content-free | UNIT TESTED | `tests/test_control_v3.py:HistoryAndStatisticsTests.test_the_timeline_is_content_free`, `test_the_timeline_code_is_the_code_the_listing_shows` | No artifact shows the timeline dialog. |
| Clear history hides and never deletes; hidden rows still count for every cap and duplicate check | UNIT TESTED | `tests/test_store.py:HideHistoryTests` (2 tests, incl. `test_T41_hidden_rows_still_count_for_every_safety_decision`); `tests/test_control_v3.py:HistoryAndStatisticsTests.test_clear_history_hides_finished_records_and_keeps_them_counting` | - |
| Statistics over 7 days, 30 days or all of it; one final outcome per record; a success rate only above five | UNIT TESTED | `tests/test_store.py:StatisticsTests` (4 tests, incl. `test_T44_a_late_receipt_counts_once`, `test_statistics_include_hidden_rows`) | The Statistics page itself has no artifact. |
| Export diagnostics writes a redacted file and never overwrites | UNIT TESTED | `tests/test_diagnostics.py:DiagnosticsTests` (8 tests) | Only `diagnostics.write` is tested. The button, the file picker and the read-before-you-send flow are unverified. |
| Repair installation reports which of five things happened, under the installer's own lock, with `--keep-state` | REAL WINDOWS TESTED | `tests/test_gui_decisions.py:DecisionTests` (3 of 11 tests) call the compiled window's own `RunRepair` against real child processes standing in for setup: each exit code and line, a setup that is missing, and the lock held by another process - including the case where setup exits 2 because it rejected `--keep-state`, which must not read as success; `tests/test_convergence.py:InstallerLockTests` (3 tests) cover the lock and `tests/test_plugin.py:BridgeTests.test_keep_state_repairs_an_autostart_that_is_already_ours` the flag | The "still working" outcome is driven by a timeout in the test rather than by a real slow setup, and the trimmed failure tail is not compared. |
| Stop watcher asks through the stop event, never kills, and reports stopped / still finishing / not running / unknown | UNIT TESTED | `tests/test_control.py:StopWatcherTests` (9 tests, incl. `test_the_stop_is_asked_once_and_nothing_is_ever_killed`, `test_a_probe_that_cannot_tell_is_unknown_and_never_stopped`, `test_the_layer_holds_no_way_to_end_a_process`) | The window's button is not driven, and the four wordings appear in no artifact. |
| Start watcher confirms through the mutex probe and never overclaims | UNIT TESTED | `tests/test_control.py:StartWatcherTests` (6 tests), `WatcherConfirmationTests` (8 tests), `StartWatcherReportingTests` (6 tests, incl. `test_every_outcome_has_wording_that_does_not_overclaim`) | - |
| The wire between window and bridge is UTF-8 whatever the machine's code page | REAL WINDOWS TESTED | `tests/test_bridge_encoding.py:WireEncodingTests` (6 tests) run the real bridge in a subprocess with `PYTHONIOENCODING=cp949` and UTF-8 mode off | The decoding half is asserted against the C# source, not against the running window. |
| A malformed or deeply nested reply is an ordinary format error, not a process death | REAL WINDOWS TESTED | `tests/test_gui_json.py:ParserRobustnessTests` (5 tests) compile the real window with the in-box C# compiler and run a probe process: `test_the_probe_process_survived`, `test_the_limit_is_where_it_says` | Skipped where the in-box compiler or PowerShell is absent. |
| Layout: a settings row measured again when its editor's height or a font changes, the footer as tall as its save-bar card, the numeric inset, buffered painting | UNIT TESTED, with a real capture behind it | `tests/test_gui_layout.py:RowLayoutTests` (4), `FooterTests` (3), `NumericInsetTests` (5), `BufferedPaintTests` (2). The module records how the clipped drop-downs were measured with the real sources at every scaling, and the audit in the next row measures the real window | These are source-shape assertions, and the earlier measurements are described in docstrings, not stored as artifacts. Only the 144 DPI case has a committed capture. |
| Nothing is cut off: every page and Settings section of the real window, at its opening size, in all nine languages at five scalings, has no control reaching past a container that does not scroll, no page that would scroll sideways, no drop-down that is not one field high, and no text, list columns or status light needing more room than they are drawn in | REAL WINDOWS TESTED | `tests/test_gui_layout.py:LayoutAuditTests.test_nothing_is_cut_off_on_any_page_in_any_language_at_any_scaling` compiles the window with the in-box compiler and has its own `LayoutAudit` build and lay it out without showing it, with the Custom message and its per-kind editor showing so every drop-down on the Settings page is laid out; `test_the_audit_finds_what_does_not_fit` hands it a label that cannot fit and requires a report; `tests/test_gui_v064.py:MaterialTests.test_a_drop_down_reports_the_height_it_really_has` measures a drop-down before and after it has a window, after a font change and after its window is made again | Skipped where the in-box compiler or PowerShell is absent. Other scalings are stood in for by scaling the window's scale and its fonts together, not by changing Windows' DPI, so system metrics such as a scroll bar's width stay as they are; the fonts are the machine's own; the lists hold no rows; and nothing is shown or photographed. |
| The window opens at 1000 × 664, a size its Overview fits whole in every language at every scaling: what the Overview's rows need at 100%, with the spacing scale's medium step (SpaceM) of room in every card and the page's gap between cards (PageGap) under the last row. On a screen whose work area is shorter - a 1920 × 1080 screen at 150% with the taskbar, for one - it opens as tall as the work area, and the Overview still fits there in every language. It does not grow to fit its tallest Settings section, and a page that scrolls keeps room for a card's lift at every scaling | UNIT TESTED, with the Overview measured in the real window | `tests/test_gui_layout.py:WindowCompositionTests.test_the_window_opens_at_a_size_the_overview_fits_and_does_not_grow`, `test_a_scrolling_page_keeps_room_for_a_cards_lift`; `tests/test_gui_layout.py:LayoutAuditTests.test_nothing_is_cut_off_on_any_page_in_any_language_at_any_scaling` measures the fullest Overview at that size, `test_the_overview_scrolls_only_in_a_window_shorter_than_its_rows_own_tallest_cards` requires the shortest window the Overview fits at 150% to fit that screen's 952 device pixels in every language, and `test_the_audit_finds_an_overview_that_would_scroll` shows the audit would say so | Source-shape: the size and the padding are read from `gui/SettingsApp.cs`, the window's frame at 150% (22 by 56 device pixels) and the taskbar (72) are figures measured on one Windows 11 machine and written into the test, and the padding is compared with brand's shadow reach at five scalings. A screen whose work area is shorter than the rows need at its scaling scrolls the Overview; which screens those are is worked out from these figures, not measured on them. No test scrolls a section, and nothing records the window opened at that size on a desktop. |
| Switching is quick without skipping a read: pages and Settings editors are built when they are first needed, painting is paused around a page or section swap and started again in `finally`, a swap hides and shows rather than removing and adding, no font is made on a switch, nothing in the window is see-through, a page refreshed in the last two seconds is not asked again, and a first visit with an older snapshot reads again | UNIT TESTED, and the snapshot rule in the real window | `tests/test_gui_layout.py:WindowCompositionTests.test_pages_and_settings_editors_are_built_when_they_are_needed`, `test_both_swaps_stop_painting_and_start_it_again_in_finally`, `test_a_swap_hides_and_shows_rather_than_removing_and_adding`, `test_fonts_are_not_made_on_every_switch`, `test_nothing_in_the_window_is_see_through`, `test_a_page_switch_does_not_ask_again_for_a_snapshot_it_has_just_had`; `LayoutAuditTests.test_a_first_visit_to_a_page_reads_again_when_the_snapshot_is_old` switches the unshown window's pages with a snapshot just read and with one 2.2 seconds old | The rest are source-shape assertions. **No test measures speed.** The timings in `CHANGELOG.md` - the first painted header from about 1.5 s to 0.45 s, page switches from a median of about 100 ms to 22 ms - were measured on instrumented scratch builds at 150% on an empty installation during development, not on the installed build, and are not stored as artifacts. |
| A page or section built or updated out of sight shows what it was last told once it is shown: the Custom message card follows the message style, and History's thread button follows its row | REAL WINDOWS TESTED | `tests/test_gui_layout.py:LayoutAuditTests.test_the_custom_message_follows_the_style_when_it_is_built_out_of_sight`, `test_historys_thread_button_follows_its_row_while_history_is_hidden` read each control's own visibility through reflection in the unshown window; `WindowCompositionTests.test_nothing_out_of_sight_keeps_a_state_it_was_told_to_drop` refuses, in `gui/SettingsApp.cs` and `gui/Dashboard.cs`, a `Visible` or `Enabled` write skipped because the getter already agreed | Skipped where the in-box compiler or PowerShell is absent. Two controls are driven; the rule for the rest is a pattern over the source. |
| The window keeps its interface text in `config\strings-cache.json`, beside the settings and nowhere else, and uses it only while it still describes the installation: a changed catalog (even one of the same length with its time put back), a changed settings file or a changed `CODEX_AUTO_RESUME_LANG` gives another key, a damaged file is a miss, a reply in a language the settings do not store is neither kept nor used, and one that comes back after the settings changed is not kept | REAL WINDOWS TESTED | `tests/test_gui_layout.py:LayoutAuditTests.test_the_strings_cache_is_used_only_for_the_installation_as_it_was`, `test_the_strings_cache_is_kept_beside_the_settings_and_nowhere_else`, `test_a_strings_reply_is_kept_and_used_only_in_the_language_the_settings_store`, `test_a_reply_that_comes_back_after_the_settings_changed_is_not_kept` drive the window's own `StringsCache` in a temporary installation laid out as the real one is, with no bridge; `WindowCompositionTests.test_the_strings_cache_is_checked_before_it_is_used`, `test_the_strings_cache_is_kept_in_config`, `test_the_strings_cache_is_written_under_a_short_name` | Skipped where the in-box compiler or PowerShell is absent. The installation is a few files in a temporary folder: nothing records the cache across a real upgrade, a real change of Windows' display language or a real uninstall, and that Windows' languages are part of the key is read from the source. No test reads the file for what `PRIVACY.md` says it holds. |
| Nothing is written in raw pixels; the column holding the state dot scales too | UNIT TESTED | `tests/test_brand.py:WindowScalingTests` (3 tests); `tests/test_screenshots.py:PixelTests.test_every_window_screenshot_shows_the_state_dot`, `test_every_window_screenshot_has_its_header_and_footer_cards` | Only one scaling has a committed screenshot. |
| The window writes no colour of its own; every colour comes from the palette | UNIT TESTED | `tests/test_brand.py:SurfaceTests.test_settings_window_writes_no_colour_of_its_own`, `GeneratedFileTests.test_gui_brand_cs_is_current`; `tests/test_brand.py:RetiredColourTests.test_no_tracked_file_still_carries_a_retired_colour`; `tests/test_gui_v063.py:KeptInStepTests.test_the_soft_controls_take_every_colour_from_the_palette` | - |
| No local web server, and nothing opens in a browser | UNIT TESTED | `tests/test_surface_properties.py:NoBrowserTests` (3 tests) refuse a browser control, an embedded renderer, a listening socket or a second runtime in every tracked file under `gui/`, `src/`, `scripts/` and `install/`, require the window to be compiled against only System, System.Drawing and System.Windows.Forms, and require the release to carry no second runtime; `tests/test_privacy_claims.py:CodePropertyTests` covers the Python | A deny list: it refuses the ways this could arrive that are known, and a new one would have to be added to it. |
| The lists refresh every five seconds | IMPLEMENTED | `gui/Dashboard.cs` (`StartClock`, `RefreshNow`, `UpdateCountdowns`) | Unverified. Nothing measures the interval, or that a refresh cannot land between a confirmation and the action it confirms. |
| The header's status light says what the watcher is doing - monitoring, waiting, checking a task that has come due, recovering, paused or needing attention - in a word beside a flat dot: cyan for every running state, grey when paused or when the watcher is stopped or unknown (before the first snapshot too), amber only for a running watcher that needs a person; while monitoring or recovering the dot dims toward the card and comes back with nothing spreading, and only once it is lit does a glow spread 3 px past it and draw back in, while waiting it holds lit, while checking it turns an arc, for an alarm it runs that cycle once, and with motion reduced it holds lit with no glow | REAL WINDOWS TESTED | `tests/test_gui_v063.py:AliveStateTests` (7 tests) compile the window with the in-box C# compiler and call its own static decision, opacity and dimming methods through reflection, for fourteen combinations of watcher status and pending work and eight moments in time, with reduced motion off and on; `tests/test_gui_v064.py:MaterialTests.test_the_glow_is_brands_for_every_state_and_moment`, `test_the_dot_is_brands_colour_for_every_state` hold the compiled light to `brand.glow` and the palette, and `test_the_drawn_glow_grows_out_from_under_the_dot_as_far_as_its_spread_and_the_dot_dims` draws the window's own light into a bitmap at 300%, its clock stopped, and measures how far the glow reaches and how far the dot is dimmed at each moment of the cycle; `tests/test_gui_layout.py:WindowCompositionTests.test_a_stopped_watcher_is_grey_before_there_is_a_snapshot_too`; `tests/test_gui_v063.py:ReviewedRulesTests.test_a_refresh_decides_the_header_dot_once`, `test_a_restored_window_starts_the_halo_again` | Skipped where the in-box compiler or PowerShell is absent. The methods are called and the light is drawn into a bitmap; the window is never shown and no capture of the moving light on a real screen exists. Reduced motion is passed in directly, so Windows' own animation setting is not what these tests vary. |
| The window is drawn in the panel's material: every shadow it stamps is `brand.shadow_alpha`'s Gaussian within two levels of 255 at 100%, 150% and 200%, a composited card, control and well match brand's model, a long edge has no seam, a ground keeps free the reach brand gives, a card keeps no band for its shadow, a number field is a well around the real spin box, and the primary button's hover and pressed colours are brand's | REAL WINDOWS TESTED | `tests/test_gui_v064.py:MaterialTests` (11 tests) compile the window and have its own drawing code stamp shadows into bitmaps and answer through reflection; `SourceRuleTests` (6 tests) refuse unsafe code and initialized constant arrays, and require every ground and lifted control to paint behind its corners and the grounds to be buffered and opaque | Skipped where the in-box compiler or PowerShell is absent, and the button colours on a machine running High Contrast. Shadows are compared across the middle of each edge and in one composite per recipe, not across a page against the panel; hover, press and keyboard focus are what the tests ask the code for, never seen, and no capture shows them. |
| In High Contrast the window draws no shadow, its status light is a solid dot in a system colour with no glow, and text on a highlight is highlight text | UNIT TESTED, with High Contrast forced | `tests/test_gui_v064.py:SourceRuleTests.test_high_contrast_draws_no_shadow`, `test_the_status_light_is_a_flat_dot_and_its_glow_is_off_in_high_contrast`; `MaterialTests.test_the_dot_is_brands_colour_for_every_state` requires the compiled window's system colour for each state to be `brand.status_system`'s; `tests/test_gui_v063.py:ReviewedRulesTests.test_text_on_a_high_contrast_highlight_is_highlight_text` | Source-shape assertions and one compiled lookup with High Contrast asked for. Nothing runs the window in a High Contrast theme, and no capture shows it in one. |
| Why it is waiting lists the watcher's safety checks for the chosen task in the engine's own order, each with words | UNIT TESTED | `tests/test_gui_v063.py:KeptInStepTests.test_why_it_is_waiting_lists_the_watchers_checks_in_its_order`, `test_every_gate_and_result_has_words` | Source-shape: the order written in `gui/Dashboard.cs` is compared with the engine's list of gates. The panel is not drawn for a real record. |
| Pending's Auto-resume switch is bound to one task's exact interruption and conversation; a click that reaches a record which has finished, disappeared or belongs to another conversation is refused and changes nothing, and switching it on sends nothing and moves no schedule | UNIT TESTED, at the control layer | `tests/test_control_continuation.py:InterruptionSwitchTests` (5 tests, incl. `test_a_click_for_another_conversation_changes_nothing`, `test_a_record_that_is_gone_or_finished_is_refused`, `test_switching_on_sends_nothing_and_changes_no_schedule`); `BridgeCommandTests.test_the_window_reaches_all_three` | The C# half - that the switch sends the ids of the row it is drawn on - is not driven. The popup's copy of the switch is driven in a real window (§8). |
| Settings is split into General, Automatic recovery, Continuation message, Appearance and Advanced, and every setting group the window shows has a place | UNIT TESTED | `tests/test_gui_v063.py:KeptInStepTests.test_every_settings_group_the_window_shows_has_a_section`; `tests/test_settings.py:DescribeTests.test_every_entry_carries_a_group_the_interfaces_understand` | Source-shape. No capture of the new Settings page exists. |
| The Custom message box stores what was typed: blank means not set, a Windows line break becomes a plain one, and nothing is cut to length | UNIT TESTED | `tests/test_gui_v063.py:KeptInStepTests.test_the_window_never_writes_a_custom_message_it_did_not_show` | An assertion over one method's source. The refusal a message gets - a forbidden placeholder, too long, empty - is tested in `tests/test_continuation.py:CustomMessageTests`, not in the window. |
| The Overview's four cards each have their heading at the top left and what they hold under it, and the three that lead somewhere have their button at the card's bottom left, as v0.6.2 had it: in a row of its own under the last line, the spacing scale's medium step (SpaceM, 12 px) clear above it, at the bottom of whatever height the card is given; Right now lists automatic recovery, the watcher, the Codex engine and the last check, with the card's whole width for their names, and wraps none of them; Recently finished fits its names after every outcome and age is written, and stays whole when a recovery finishes with the Overview open; and Pending's Why it is waiting is as tall as the list's card beside it and scrolls inside its own card, never the page | UNIT TESTED, with the Overview measured in the real window | `tests/test_gui_layout.py:WindowCompositionTests.test_the_overview_leads_to_each_cards_button_from_its_bottom_left`, `test_recently_finished_fits_its_names_after_every_outcome_and_age_is_written`, `test_why_it_is_waiting_scrolls_inside_its_card_and_never_the_page`; `LayoutAuditTests.test_the_audit_finds_an_overview_that_would_scroll`, `test_right_now_keeps_its_names_whole`, `test_recently_finished_stays_whole_when_a_recovery_finishes_with_the_overview_open` | The composition tests are source-shape assertions. The audit measures the Overview in the unshown window; nothing measures Pending's scrolling there, and nothing records either page on a desktop. |
| The Overview's two rows of cards are as tall as their cards need and a little more: the same part for each row, never more than the spacing scale's extra-large step (SpaceXl, 24 px) past what it needs, and rows alike only where that stretches neither past it, so a row of short cards keeps its own height. Under the last row the page keeps its own rhythm, the gap between its cards (PageGap, 14 px) - neither no space at all, as the rows once left, nor a dead band - and what a taller window has past that stays as space under the rows. In a shorter window that space goes first and then the rows' part, the Overview scrolls only in a window shorter than its rows' own tallest cards, and a card that grows on a page already laid out makes the page scroll and is laid out whole | REAL WINDOWS TESTED | `tests/test_gui_layout.py:LayoutAuditTests.test_nothing_is_cut_off_on_any_page_in_any_language_at_any_scaling` has the window's own `AuditRows` report anything but the page's rhythm under the last row, cards of different heights in a row, rows that need the same given different heights, or a card that floats past what it needs, in all nine languages at five scalings, and `test_the_overview_neither_scrolls_nor_moves_whatever_state_the_watcher_is_in` at every state; `test_the_audit_finds_an_overview_laid_out_otherwise` shows it rows sharing the page's whole height as they once did, rows over an empty band and cards floating in a much taller window, and requires each report; `test_the_overview_scrolls_only_in_a_window_shorter_than_its_rows_own_tallest_cards` requires `AuditShortest` to have measured the shortest window the rows' own tallest cards fit, in every language at every scaling, and to have found the page there unscrolled with no space under its rows, no card cut short, every button in its corner over no text and nothing cut off, and a window a pixel shorter scrolling; `test_the_overviews_rows_are_as_tall_as_their_cards_and_a_little_more` checks `OverviewHeights` in nine cases; `test_a_card_that_grows_on_an_overview_already_laid_out_scrolls_the_page_and_is_laid_out_whole`; `WindowCompositionTests.test_the_overviews_rows_hold_their_cards_and_keep_the_pages_rhythm_under_them` | Skipped where the in-box compiler or PowerShell is absent. The window is built and never shown, and other scalings are stood in for as in the audit above; the rows and the space under them may be up to 3 pixels from what `FitOverview` gives them, for rounding; the shortest window is worked out from the cards' own preferred heights, not taken from a real screen's work area; the canaries and the grown card run in English at this machine's scale; the composition test is a source-shape assertion. Whether the space reads as design rather than emptiness is a person's judgement, which no test makes, and nothing records the Overview at the opening size on a desktop. |
| The Overview's Pause or Resume recovery, Pending and History stand at their card's bottom left, and the window's other buttons at the right of a card or a row - the header's Start watcher and the Custom messages' two Clear buttons - are pinned to its bottom right. In every language at every scaling each is within a pixel of its card's or row's inner corner and over no line of text, and no line of text is closer above an Overview button than the spacing scale's medium step (12 px at 100%); a card's content comes before its button in the order Windows hands a screen reader, as it does for Tab; and no window of an Overview card but the card itself lies under its button's lift or focus ring, so the card, which draws them, is the one window they are painted on | REAL WINDOWS TESTED | `tests/test_gui_layout.py:LayoutAuditTests.test_every_button_is_in_its_corner_and_over_no_text` requires the audit to have held all six to their corners in all nine languages at five scalings, and `test_the_audit_finds_a_button_out_of_its_corner_or_over_text` hands it a button that is neither, at the left and at the right, and one in its corner with a line of text just above it, and requires each report; `test_no_window_of_a_card_lies_under_its_buttons_lift_or_focus_ring` reads which windows lie under each Overview button's lift and ring; `test_a_screen_reader_reaches_a_cards_content_before_its_button` reads each card's window order and tab order from Windows; `WindowCompositionTests.test_the_overview_leads_to_each_cards_button_from_its_bottom_left`, `test_every_button_at_the_right_of_a_card_or_row_is_pinned_to_its_bottom_right` | Skipped where the in-box compiler or PowerShell is absent. The window is built and never shown, other scalings are stood in for as in the audit above, and Start watcher is made visible only for the check; text under or above a button is judged as the audit models a label's wrapping; the lift and window-order checks run in English at this machine's scale, in a window only as tall as the rows need, where each button is as close under its card's content as it ever comes. That the card repaints a lift and ring when its button's state changes is not measured. No screen reader has read the window, and no capture shows a focus ring. |
| The Overview neither scrolls nor moves whatever state the watcher is in: a page opened on a watcher in trouble with recovery paused, refreshed through the fullest reply, a stopped watcher, one in an unknown state, nothing waiting and unreadable lists, then made shorter than it needs and taller again, fits, keeps the page's rhythm under its rows, keeps its buttons in their corners, wraps none of Right now's names and keeps Right now's size and the height it needs at every step; and a page shows its soft bar by what fits without it, not by whether the bar showed before | REAL WINDOWS TESTED | `tests/test_gui_layout.py:LayoutAuditTests.test_the_overview_neither_scrolls_nor_moves_whatever_state_the_watcher_is_in` has the window's own `OverviewStatesAudit` apply each reply in turn in all nine languages at five scalings, with its rows and buttons audited at every step, and requires every state and the round trip to have been measured; `test_the_state_audit_finds_right_now_laid_out_for_words_it_was_not_planned_for` hands it an engine state the window has no word for, a paragraph long, and requires a report; `test_the_state_audit_finds_a_line_of_right_now_that_moved` hands it one a line longer, which still fits and leaves the page unscrolled, and requires the height Right now needs to report the move; `WindowCompositionTests.test_a_page_shows_its_bar_by_what_fits_without_it_and_not_by_what_showed_before` | Skipped where the in-box compiler or PowerShell is absent. Only the catalog's words are measured: Right now's names have the card's whole width and are no longer laid out ahead for the widest words, so an engine state with no word that takes another line makes the card a line taller, which the audit reports as a move. The window is never shown, and no refresh is recorded on a desktop. |
| Pages, Settings sections, Why it is waiting and every list scroll on the window's own soft bar and never on Windows' own: the thumb as long as what shows and travelling the whole track, a drag landing on the offset it stands for, a wheel notch three lines and a press on the track a page, the wheel over a child or a drop-down scrolling the page, a control the keyboard moves to scrolled into view moving as little as it can, a glide that arrives without overshooting and no glide with motion reduced, the bar only while there is more than fits, the panel's material a step darker under the pointer, system colours with no shadow in High Contrast, and a list's own bar hidden behind it | REAL WINDOWS TESTED | `tests/test_gui_v064.py:SoftScrollTests` (14 tests) compile the window and drive its own page and list hosts unshown, sending the wheel as the message Windows sends and raising focus as the control's own Enter; `SourceRuleTests.test_the_soft_bar_has_no_shadow_in_high_contrast_and_no_native_bar_behind_it`; `tests/test_gui_layout.py:WindowCompositionTests.test_nothing_in_the_window_scrolls_on_windows_own_bar` | Skipped where the in-box compiler or PowerShell is absent. The pages hold stand-in content and are never shown; no person's wheel, drag or keyboard is recorded, and no capture shows the bar moving or in High Contrast. |
| The window's theme is the Theme setting, and *Use system setting* is Windows' app mode - dark only when `AppsUseLightTheme` is 0 - read where Windows keeps it; the stored setting is read before the first control exists, exactly as the settings layer reads the same file, so a value it does not know is *Use system setting*; and High Contrast, asked of Windows each time rather than of a cached answer, wins over every choice | REAL WINDOWS TESTED | `tests/test_gui_theme.py:WindowThemeTests.test_the_theme_is_the_setting_then_windows_app_mode_and_high_contrast_wins` calls the compiled window's own resolution for eight stored values under four app modes, with High Contrast off and on; `test_the_app_mode_is_read_where_windows_keeps_it` compares its registry read with Python's `winreg`; `test_the_stored_theme_is_read_as_the_settings_layer_reads_it` writes settings files - among them a byte-order mark, an ANSI file, trailing content and one too large - and requires the window's answer to be `settings.load`'s; `test_high_contrast_is_asked_of_windows_not_of_a_cache_the_change_has_not_reached`; `ThemeSourceRuleTests.test_the_theme_is_adopted_before_the_first_control_is_made`, `test_high_contrast_is_asked_of_windows_each_time` | Skipped where the in-box compiler or PowerShell is absent. The app mode read is whatever this machine's registry holds, and the other app modes are passed in; nothing records Windows' app mode or High Contrast being switched for real. |
| In dark the window is the panel's dark theme: every palette colour is `brand.DARK`'s, a card is lifted off its surface to `brand.card_ground`, every shadow is the dark recipe - three for a card, one of them inside its top edge - composited as brand's model has it, light's shadows stay exactly as they were, no control on any page keeps a light colour, the version in the save card is readable text in both themes, and the title bar asks Windows for its dark style; in High Contrast every colour is the system colour, whatever the theme | REAL WINDOWS TESTED | `tests/test_gui_theme.py:WindowThemeTests.test_each_theme_draws_from_its_own_palette`, `test_the_dark_card_is_lifted_off_its_surface_as_the_panels_is`, `test_the_dark_shadows_are_the_panels_recipes`, `test_a_dark_body_is_composited_as_the_panel_model_has_it`, `test_the_light_elevation_is_unchanged_by_the_dark_one`, `test_nothing_in_the_dark_window_keeps_a_light_colour` - which builds and fills every page unshown in dark and reads every control's colours - `test_the_version_in_the_save_card_is_readable_text`, `test_high_contrast_is_system_colours_whatever_the_theme`; `ThemeSourceRuleTests.test_no_light_colour_is_drawn_outside_the_theme_it_was_adopted_from`, `test_every_theme_dependent_brand_rule_is_read_with_its_dark_twin`, `test_no_colour_is_written_as_a_literal_or_a_named_colour`, `test_every_elevation_reads_each_shadows_own_inset_flag`, `test_the_version_is_text_not_a_fill`, `test_the_title_bar_goes_dark_with_the_immersive_dark_mode_attribute` | Skipped where the in-box compiler or PowerShell is absent. No committed picture shows the dark window, and the title bar's attribute is read from the source, not seen. Message boxes, the file dialog, a text box's context menu and an open drop-down's frame are Windows' own and stay light; no test says which. High Contrast is adopted by the probe, not switched on in Windows. |
| A setting is a check box exactly when it picks items of a list - `recover_<kind>` and `notify_<event>` - and a switch when it turns something that runs on or off; the built window draws each as its kind with the glyph left of the label; and the check box is `brand.CHECKBOX` in each of its four states in light and in dark, and `brand.CHECKBOX_SYSTEM` with no shadow in High Contrast | REAL WINDOWS TESTED | `tests/test_gui_theme.py:WindowThemeTests.test_a_setting_is_a_check_box_exactly_when_it_picks_items_of_a_list`; `test_the_built_window_draws_each_setting_as_its_kind_with_the_glyph_on_the_left` builds the Settings sections unshown and reads each control's kind and where its glyph is; `test_the_check_box_is_brands_in_light_and_dark` and `test_the_check_box_in_high_contrast_is_system_colours_with_no_shadow` draw the box into bitmaps and read the pixels back; `ThemeSourceRuleTests.test_the_check_box_is_one_control_class_with_a_kind` | Skipped where the in-box compiler or PowerShell is absent. The box is drawn into a bitmap and never clicked, and hover and keyboard focus are drawn only as the tests ask. The same rule in the panel is §9's. |
| Theme is a drop-down of its three choices in their own words; unsaved edits are exactly what differs from the page as built or last saved; and a Save sends the Interface language and the theme only when they were changed on the page, so it cannot put back a value changed elsewhere in the meantime | REAL WINDOWS TESTED | `tests/test_gui_theme.py:WindowThemeTests.test_the_theme_is_a_drop_down_of_its_three_choices_in_their_own_words`, `test_unsaved_edits_are_what_differs_from_the_page_as_built`, `test_a_save_sends_the_language_and_theme_only_when_they_were_changed_here` build the Settings page unshown in light and dark and ask its own methods | Skipped where the in-box compiler or PowerShell is absent. Every other setting is still sent whole, so one changed elsewhere while the window is open can still be overwritten by a Save, as before. |
| The window reopens itself after a change of the Interface language or the theme - after its own Save, after a change made elsewhere that a settings read finds, and when Windows' app mode or High Contrast changes - but never over unsaved edits, when a note in the save card, whole in every language, says it will reopen once they are saved or discarded; a disagreement that never goes away reopens it at most twice; it closes only once the new window is up, and a new window that never shows leaves it working; the new window opens on the same page and section, wholly on screen by the frame one sees, with the keyboard where it was, from arguments it checks value by value; and no single-instance rule refuses it | REAL WINDOWS TESTED, as the window's own decisions and its own ending of a handover | `tests/test_gui_theme.py:WindowThemeTests.test_which_changes_reopen_and_which_wait_for_the_edits` calls the compiled window's own decision for every case; `test_the_window_waits_for_unsaved_edits_and_says_so_in_the_save_card` walks its real `CheckReopen` through ten settings reads with an action marked as running, so nothing is started; `test_a_disagreement_that_never_goes_away_reopens_at_most_twice`; `test_a_window_whose_new_one_never_showed_stays_and_goes_on_working` and `test_the_wait_for_the_new_window_ends_the_way_its_process_says` call its own ending and wait with the answers a new process would give; `test_a_reopened_window_puts_the_keyboard_where_the_old_one_had_it`; `test_the_reopen_note_is_whole_beside_the_buttons_in_every_language` measures the note at the opening width and at the narrowest the window goes, in all nine languages; `test_the_command_line_is_checked_value_by_value`, `test_a_reopen_passes_on_exactly_what_the_new_window_accepts`, `test_a_reopened_window_is_placed_wholly_on_its_screen`; `ThemeSourceRuleTests.test_the_window_reopens_on_a_save_a_change_elsewhere_and_windows_app_mode`, `test_the_window_has_no_single_instance_rule_to_refuse_the_new_one`, `test_the_note_about_a_pending_reopen_sits_in_the_save_card` | **No handover is run.** No test starts the new process, waits for its window and closes the old one, and nothing in this repository records a reopen after a real Save, a real change in the panel or in Codex, or a real change of Windows' app mode or High Contrast. A change made elsewhere is noticed from the settings file's time and size every two seconds, which a rewrite of the same time and size would slip past. Skipped where the in-box compiler or PowerShell is absent. |
| The Diagnostics page's Codex compatibility card shows the watcher's report as the bridge read it - the overall word, the Codex version, when it was checked and which data is in force by its number - with each capability the product offers and its state in words, and each word it shows explained; a report that cannot be used is one notice and no parts, a failed read is said and never guessed at, and the live check a refresh brings stands in only until a report as new arrives and never longer than a report may be relied on | REAL WINDOWS TESTED | `tests/test_gui_compat.py:CardTests` (17 tests, incl. `test_a_report_that_cannot_be_used_says_why_and_lists_nothing`, `test_a_refreshs_live_check_stands_until_a_report_as_new_arrives`, `test_every_answer_is_said_as_itself`) compile the window and drive the card through reflection with views the real bridge produced for the registry's hermetic fixture; `WordsTests` (5 tests) | Skipped where the in-box compiler or PowerShell is absent. The card is built and read, never shown; no capture shows it after a refresh. |
| Refresh compatibility data is the only request the window makes for the registry, and only its button asks: it goes over the one-shot bridge from a worker thread and never over the long-lived pipe, it waits while an installation or a repair holds the installer's lock without ever holding it, and the refresh an update check makes reaches the card in the same words | UNIT TESTED | `tests/test_gui_compat.py:RefreshRouteTests` (9 tests, incl. `test_only_its_button_asks_for_it`, `test_busy_looks_at_the_installers_lock_and_never_holds_it`, `test_an_update_checks_compatibility_line_reaches_the_card`) | Assertions over `gui/Dashboard.cs`. Nobody has pressed the button against the real address. |
| The window's drop-down lists open the product's own list and never Windows': every way Windows' list could open opens this one; the keys move the highlight and choose nothing until Enter, and while it is open they are the drop-down's, not the window's; typing finds the next item that starts with it; it opens under the field with its words under the field's, above it near the screen's bottom edge, and scrolls where neither side has room; it never activates and its shadow lets a click through; a click elsewhere, the window leaving or moving, or the drop-down hiding closes it; the wheel scrolls only an open list; the chosen item, the item under the pointer and the one the keyboard is on are told apart; a screen reader and UI Automation read a combo box with its choice and hear the item the highlight is on; it fades in and rises unless motion is reduced; dark is the cards' dark material, and High Contrast is system colours with no shadow | REAL WINDOWS TESTED | `tests/test_gui_v065_controls.py:ControlsTests` (fifteen of its twenty tests, incl. `test_every_way_windows_list_could_open_opens_this_one_and_windows_list_never_shows`, `test_ui_automation_reads_a_drop_down_as_a_combo_box_with_its_choice_that_it_can_open`, `test_it_fades_in_and_rises_and_with_motion_reduced_it_is_simply_there`) compile the window and drive the real control; `SourceRuleTests.test_every_message_that_opens_windows_list_is_answered_by_the_drop_down`, `test_the_list_never_activates_and_has_no_shadow_in_high_contrast` | Skipped where the in-box compiler or PowerShell is absent. The list is driven through its own messages and read back; nobody has used it with Narrator on a real desktop, and no capture shows it open. |
| In the window a switch glides - its knob slides and its track cross-fades, in brand's 160 ms on brand's one ease-out curve - and a check box fades; at rest both are exactly what they were; Pending's Auto-resume switch glides only once its change is confirmed, repaints its own cell and lays out nothing; and a change is drawn in its new place at once where it cannot be seen, with Reduce motion or with Windows' animation setting off | REAL WINDOWS TESTED | `tests/test_gui_v065_controls.py:ControlsTests.test_every_change_moves_on_brands_one_transition_and_curve`, `test_at_rest_the_switch_and_the_box_are_exactly_what_they_were`, `test_the_knob_slides_and_the_track_cross_fades_and_the_box_fades`, `test_a_real_switch_glides_then_stops_and_is_immediate_when_it_cannot_be_seen_or_motion_is_reduced`; `tests/test_gui_v065_window.py:WindowReviewTests.test_the_pending_switch_glides_once_its_change_is_confirmed`, `test_the_pending_switch_is_immediate_when_windows_reduces_motion`, `test_the_glide_is_brands_transition`; `WindowSourceTests` (4 tests); `tests/test_brand.py:GeneratedStatusLightTests.test_the_window_eases_a_transition_as_brand_ease_does` | Skipped where the in-box compiler or PowerShell is absent. Frames are asked for at chosen moments; nothing measures a glide on screen, and High Contrast is held by a source rule. |
| A list's columns fit its width at every ordinary size: what a column holds gives way only after the headings, and only until it is no longer readable, no column keeps room past what it holds while another is cut short, and what no longer fits ends in an ellipsis; only a list narrower than its columns can shrink to scrolls sideways, on the soft bar, with Windows' own horizontal bar hidden | REAL WINDOWS TESTED | `tests/test_gui_v065_window.py:WindowReviewTests.test_what_a_column_holds_gives_way_after_the_headings_until_it_is_no_longer_readable`, `test_no_column_keeps_room_past_what_it_holds_while_another_is_cut_short`, `test_the_reviews_german_list_reads_its_status_and_kind`; `tests/test_gui_v065_controls.py:ControlsTests.test_a_list_wider_than_its_host_scrolls_sideways_on_the_soft_bar_and_hides_windows_own` | Skipped where the in-box compiler or PowerShell is absent. Widths are computed by the compiled window and read back at the widths the tests choose. |
| Pending's and History's lists are flat rows on the card, closed by a hairline, at the pitch they had: the chosen row is filled softly rather than pressed into a well, with the keyboard's mark at its left end; each switch stands where its column puts it; every heading starts where its cells do; and nothing to list is a line over the card, the list keeping its headings | REAL WINDOWS TESTED | `tests/test_gui_v065_rows.py:RowTests` (9 tests), `RowSourceTests` (3 tests) | Skipped where the in-box compiler or PowerShell is absent. Rows are drawn into bitmaps and read back; the screenshots are the only pictures. |
| The window breaks a Korean line only at a space, each line as long as fits, and leaves text with no Korean to Windows; help notes and values wrap this way, and what draws its own words wraps them the same way | REAL WINDOWS TESTED | `tests/test_gui_v065_words.py:WordTests` (5 tests), `WordSourceTests` (3 tests) | Skipped where the in-box compiler or PowerShell is absent. Measured with the window's own wrapping code, not in a capture. |
| A bridge command that takes a switch takes a JSON boolean or a coded refusal: `"false"`, `"true"`, `0`, `1`, `null` and the like are refused for every such command, a missing value is refused rather than read as off, a string never reaches the sign-in autostart writer, and what the window actually sends is accepted as it is; no test built on the control layer can reach the real `Run` key | UNIT TESTED | `tests/test_control.py:SwitchFlagTests` (6 tests), `RegistryIsolationTests` (2 tests) | - |

## 8. The notification-area icon

| Capability | Evidence | What backs it | What is still missing |
| --- | --- | --- | --- |
| The icon belongs to the watcher process: it appears when one starts and goes when it stops | REAL WINDOWS TESTED | `tests/test_tray.py:LiveIconTests.test_the_icon_starts_updates_and_goes_away_cleanly` creates a real notification-area icon on Windows, updates its tooltip and requires its thread to end; `tests/test_tray_popup.py:WindowsTests.test_the_icon_starts_with_the_popup_wired_and_stops_cleanly` does the same with the popup wired and requires the waiting badge on the icon | Skipped off Windows. No screenshot of the icon or its menu exists anywhere in the repository, and the popup's pictures (`docs/images/tray-popup.png`) are drawn by its renderer off-screen rather than captured from the icon. The icon coming back after Explorer restarts is implemented - its window listens for `TaskbarCreated` in `src/codex_auto_resume/tray.py` - and no test exercises it. |
| The tooltip says paused / waiting / running / next check, in every language, within the tooltip limit | UNIT TESTED | `tests/test_tray.py:WordingTests` (4 tests, incl. `test_every_language_fits_the_tooltip_limit`, `test_the_countdown_is_short_and_never_negative`) | Nobody has recorded reading the tooltip in any of the eight translations on a real machine. |
| What it shows comes from this product's own store and nothing else | UNIT TESTED | `tests/test_tray.py:SnapshotTests.test_the_snapshot_comes_from_our_own_store_only` | - |
| Its right-click menu opens the window, pauses or resumes recovery and stops the watcher, all through the same control layer | UNIT TESTED | `tests/test_surface_properties.py:TrayMenuTests` (9 tests) dispatch every menu id through the icon's own `_act`: open, stop, the one item that means pause or resume depending on the state it was drawn in, closing the menu without choosing, an id that is not an item at all, an action that throws - which must be logged rather than take the watcher down with it - and an icon with nothing wired to it; the other two are the route to Pending in the next row | The ids are dispatched, not clicked: `TrackPopupMenu` itself is not driven, so which id a person's click produces is still unverified. |
| The menu offers a route to the exact pending work, and not the actions on it | UNIT TESTED | `src/codex_auto_resume/tray.py` (`MENU_PENDING`, `open_dashboard`) adds an item, only while something is waiting, that opens the window on its Pending page; `tests/test_surface_properties.py:TrayMenuTests.test_pending_opens_the_page_where_those_actions_live`, `test_the_window_is_only_ever_opened_on_a_page_it_has` | **Retry now and Cancel are deliberately not in the menu.** A context menu built from a list the watcher is still changing would act on whichever record an id meant when the menu was drawn, and a menu item has nowhere to name the conversation it is about - which is exactly what the window's confirmations use. The route is the safer half of the same need. `tests/test_tray_popup.py:SafetyTests.test_the_menu_still_offers_nothing_per_task` holds that after v0.6.3; the per-task switch lives in the popup below, where each row carries the conversation's name and its exact ids. |
| It is on by default and can be switched off in the settings | UNIT TESTED, for the setting | `tests/test_settings.py:DefaultsTests.test_every_field_has_a_default`, `DescribeTests.test_describes_every_field_exactly_once` | Nothing checks that switching it off actually removes the icon. |
| A single click on the icon opens a small window beside it and a second click closes it; the click that closed it by taking focus away does not reopen it, a double click leaves it open, and Enter on the icon counts once | UNIT TESTED | `tests/test_tray_popup.py:SelectTests` (4 tests) | A pure decision over click times. The messages Explorer sends for a real click are not driven. Closing on Esc and on losing focus, and a double click opening the Dashboard as before, are implemented and exercised by no test. |
| What it shows: the most urgent tasks first and at most three, each counting down to its reset or next try, only the conversation's name taken from the conversation, a switched-off conversation's switch off, pause or resume by the state it was drawn in, and every word from the catalog | UNIT TESTED | `tests/test_tray_popup.py:ViewModelTests` (11 tests), `ActivityTests` (3 tests) | Built from records written in the test. |
| Where it goes: beside the icon for a taskbar on any edge, on a monitor left of or above the primary one, with an auto-hidden taskbar, and always inside the work area | UNIT TESTED | `tests/test_tray_popup.py:PlacementTests` (10 tests) | The rectangles are the test's. Nothing in this repository records it placed on a real multi-monitor desktop or beside an auto-hidden taskbar. |
| Its layout: every target inside the card at every scale and in every language, wrapped text given the height it measured, Tab and Shift+Tab cycling, each task's switch pinned to the bottom right of its row - level with its label's last line however far the label wraps, a one-line row exactly as it was, and nothing else on the row under the switch - the header in its place with the glow kept on the card, the card's lift faded into the canvas by the window's edge, and a typeface for each script | UNIT TESTED | `tests/test_tray_popup.py:LayoutTests` (11 tests, incl. `test_a_switch_sits_at_the_end_of_its_label_s_last_line`), `FontTests` (4 tests); `tests/test_tray_popup_v064.py:SwitchPlacementTests` (8 tests) hold the switch to the row's inner right edge and to the bottom of its row beside its label's last line, and keep the label and everything else on the row clear of it, in all nine languages at every scale from 100% to 250%, with each language's own labels and with each said three times over | Text is measured by a stand-in with a fixed advance per character, not by the real renderer; `DarkRendererTests` below repeats the placement checks with GDI's measurements. |
| A click acts only on the task its row was drawn with; a record that changed first is refused, changes nothing and says so; one switch cannot be pressed twice while under way; Open Dashboard asks the control layer for nothing | UNIT TESTED, and once in a real window | `tests/test_tray_popup.py:ActionTests` (11 tests, incl. `test_a_list_that_re_sorted_after_drawing_does_not_move_the_click`, `test_every_stale_refusal_changes_nothing_and_says_so`, `test_open_dashboard_makes_no_control_call`) against a fake control layer; `WindowsTests.test_a_refused_click_in_the_real_window_changes_nothing` through the real window; the refusals the real control layer gives are `tests/test_control_continuation.py:InterruptionSwitchTests` (5 tests) | A fake control layer stands in for the real one in both popup tests. |
| It never reaches anything that can send: it imports nothing that submits, asks the control layer for exactly four things, drives no other program's window and opens no connection | UNIT TESTED | `tests/test_tray_popup.py:SafetyTests` (6 tests) | Source-shape assertions: they hold the shape the reasoning depends on and pass code that is wrong in a new way. |
| The real window is created, drawn and destroyed without leaking, opened and closed many times holding nothing, every GDI+ object it makes released, no shadow image added as rows, notices and a pause come and go, its badge icon made and released, Korean broken between words, and its fonts rebuilt for a new language | REAL WINDOWS TESTED | `tests/test_tray_popup.py:WindowsTests` (18 tests) create the window off-screen and compare the process's GDI and USER resource counts before and after fifty rounds, and count the module's own GDI+ objects, which those counts cannot see (`test_the_renderer_lets_go_of_every_gdiplus_object`, `test_rows_notices_and_a_pause_add_no_shadow_image`); `BadgeTests` (3 tests) | Skipped off Windows. The window is shown off-screen and never looked at: the committed pictures of the popup are drawn by its renderer, not captured from this window, and no person's click on a real icon is recorded. |
| Its status light is `brand.glow` frame for frame, as the header's is: cyan for every running state, grey with no glow when paused, amber for attention, and blinking with a glow only once lit, lit and still, turning an arc or running the cycle once by state, its cycle starting with its state; Reduce motion or High Contrast holds it lit and still with no glow, and Reduce motion wins over Windows' own setting | UNIT TESTED, and in a real window off-screen | `tests/test_tray_popup.py:MotionTests` (9 tests, incl. `test_every_frame_is_the_brand_status_light`, `test_monitoring_dims_and_only_once_lit_spreads_a_little`, `test_reduced_motion_never_loops_or_pulses`, `test_the_cycle_starts_with_the_state`), `StatusLightTests.test_the_dot_is_the_brand_status_light`, `ReduceMotionSettingTests` (2 tests); `WindowsTests.test_a_paused_dot_is_grey_with_no_glow`, `test_high_contrast_holds_the_glow_still`, `test_the_glow_grows_out_from_under_the_dot_as_far_as_its_spread_and_draws_back_in`, which draws the popup's halo at 300% through the bloom and the withdrawal and measures how far it reaches | The drawn glow is measured in the popup's own canvas; nothing measures the animation on screen, and no capture shows the glow moving. |
| It is drawn in the panel's material: along an edge a lift is brand's shadow model and a well brand's inset model, a lift is symmetric and round at its corners, light and shade move by the same whole pixels, every body longer than its corners shares one shadow image, and every colour is a brand token; a frame drawn in a real window is lifted - shade below and right of the card, light above and left | UNIT TESTED, and once in a real window off-screen | `tests/test_tray_popup.py:ElevationTests` (6 tests), `SafetyTests.test_every_colour_is_a_brand_token`; `WindowsTests.test_a_frame_is_drawn_in_the_brand_colours` reads a drawn frame back pixel by pixel | A handful of pixels in one frame at one scale are read back. Nobody has looked at the popup on a real desktop, and hover, press and focus are drawn only as the tests ask for them. |
| In High Contrast every colour is the system colour the settings window uses for it, the status light included, with no shadow and no glow, and the light holds still | UNIT TESTED, with High Contrast forced | `tests/test_tray_popup.py:HighContrastTests` (2 tests); `WindowsTests.test_high_contrast_is_system_colours_with_no_shadow_and_no_glow` draws a frame with it on and reads the pixels back, and `test_high_contrast_holds_the_glow_still` opens the window with it on | High Contrast is forced on in the tests rather than switched on in Windows: nothing records the popup on a real High Contrast desktop, or redrawn when the theme changes while it is open. The two window tests are skipped off Windows. |
| The notification-area icon and its badge did not change in v0.6.4: the same table, the same state from the same snapshot, the same pixels, and the icon's badge is the badge drawn into the icon's own pixels | UNIT TESTED, and on a real icon on Windows | `tests/test_tray_popup.py:BadgeTests.test_the_icon_s_badge_is_what_it_was_before_v0_6_4` pins the table, eleven snapshots and a digest of the badge's pixels, taken from v0.6.3's code; `WindowsTests.test_the_badge_icon_is_the_badge_drawn_into_the_icon_s_own_pixels` | The digest covers a synthetic icon at five sizes; nothing shows the icon on a real taskbar. |
| The popup's theme is the stored Theme: *Use system setting* follows Windows' app mode and is dark only when Windows says dark, Light or Dark wins over Windows, High Contrast wins over every choice, a stored value it does not know is *Use system setting*, and its language is resolved exactly as the watcher resolves it | UNIT TESTED | `tests/test_tray_popup_v064.py:ThemeResolutionTests` (7 tests, incl. `test_windows_app_mode_is_read_from_the_registry`, which compares the popup's own registry read with `winreg`) | The app mode and High Contrast are passed in; nothing records either changed for real under an open popup. |
| In dark the popup is drawn in the dark palette on the panel's dark card: its drop shadow faded into the canvas by the window's edge, its lift along the right and bottom, its top light and its wells as brand's models have them, every label fitting its column as GDI measures it, and a wrapped label's last line drawn beside its switch in both themes; High Contrast wins over dark; the ground and every shadow image are kept by theme, one mask in two themes is two images, and flipping the theme again and again holds no more images and no more GDI+ objects | UNIT TESTED, and drawn with GDI+ on Windows | `tests/test_tray_popup_v064.py:DarkElevationTests` (3 tests); `DarkRendererTests` (7 tests) draw frames with the popup's own renderer and read the pixels back, including a light, dark, light sequence whose last frame is byte for byte its first | `DarkRendererTests` is skipped off Windows. The top light is held to brand's model at 100% and 200% only; at the scalings between, it is an antialiased band. Nobody has looked at the dark popup on a real desktop, and the dark border Windows 11 draws round it is asked for, never seen. |
| The popup and the icon follow a change without the watcher restarting: the icon takes up a stored language and theme before it opens the popup or its menu, reads the settings file only when it has changed, rewrites its tooltip in the new language, and loses only the new words to a file it cannot read; *Use system setting* follows Windows on the next frame, a change of Windows' app mode repaints an open popup, a read that carries new settings changes an open popup, and a popup the icon made earlier opens in the language and theme stored since | UNIT TESTED, and in a real window off-screen | `tests/test_tray_popup_v064.py:NextOpenTests` (7 tests) store the change through the real control layer in a temporary home; `PopupThemeTests` (5 tests) open the popup off-screen with Windows' app mode and High Contrast patched | The settings-change message is sent to the test's own popup, not broadcast by Windows, and no person's click on a real icon after a real change is recorded. `PopupThemeTests` is skipped off Windows. |
| The icon's right-click menu is dark while the popup is dark: Windows is asked for dark menus before the menu is made, and for its own look again otherwise, through uxtheme by ordinal and only from Windows 10 version 1903; where Windows cannot be asked, or the request fails, the menu opens as it always did, and a failure is logged once and never tried again | UNIT TESTED | `tests/test_tray_popup_v064.py:MenuThemeTests` (5 tests), against a fake uxtheme and a fake user32 | The two calls are undocumented Windows exports, and the tests fake them. Nothing in this repository records a dark menu on the real icon, on Windows 10, or in High Contrast. |
| The icon moves in five states of its own, each a brand status-light state: watching is a loop of five slots, each one of brand's monitoring breaths long - four breaths of the head, then one clockwise turn round the ring at full brightness, eased in and out - so it never breathes while it turns, and every hand-over is at full brightness; recovering turns clockwise all the time on brand's arc rhythm, at full brightness and without breathing; paused is grey and still; attention and a failure take their colour, pulse once and hold; anything unknown is idle grey; a running icon's head is the mark's own accent; and the frame rates are the measured ones, each just inside a whole number of Windows' timer ticks | UNIT TESTED | `tests/test_tray_icon_motion.py:StateTests` (8 tests, among them `test_the_frame_rates_are_the_measured_ones_and_land_on_windows_timer_ticks`), `PhaseTests` (11 tests, among them `test_the_head_never_breathes_while_it_turns`, `test_every_hand_over_is_at_full_brightness`, `test_the_turn_comes_every_fifth_breath_and_lasts_one` and `test_the_head_turns_clockwise`), `TimerTests` (4 tests) | Frames and timers are computed and asked for. The taskbar button was watched on a real taskbar while this was built, and what explorer.exe spends on each rate measured; neither is recorded in this repository, and the notification-area icon itself was not watched. |
| A frame moves only the head, once round the ring, clockwise, over its 24 positions, and the breath is 24 levels of the head's colour, a tint and never a stored frame; the head in its place in the accent is the `.ico`'s own image; the badge is composited last, exactly as the badge icon is; a colour touches the head and nothing else; the frame table is pinned; every icon size builds well inside its budget, and one over budget is no motion rather than an exception | UNIT TESTED | `tests/test_tray_icon_motion.py:FrameTests` (6 tests), `BudgetTests` (3 tests); `tests/test_brand.py:GeneratedFileTests.test_the_icon_s_geometry_is_brand_s_under_its_old_names` | The pixels are computed, not captured from a real notification area. |
| Any one reason holds the icon still - Reduce motion, saved in the window and taken up at the next tick, Windows' animation setting, High Contrast, battery saver, a locked or disconnected session, an icon in the overflow area, or frames that could not be built - and a still state asks Windows nothing; swapping five hundred frames holds at most two icons and leaks nothing, and a paused icon is its grey frame | REAL WINDOWS TESTED, for the icon handles | `tests/test_tray_icon_motion.py:MotionRuleTests.test_any_one_reason_holds_it_still`, `ObserveTests` (6 tests), `PlacementTests` (11 tests), `StoredReduceMotionTests` (5 tests); `SwapTests` (3 tests) and `LiveMotionTests` (2 tests) on Windows | Skipped off Windows. Nobody has watched it on a real taskbar, on battery saver or across a lock. Which icons are in the overflow area is Windows' own setting for each icon (`IconPlacement`), because on Windows 11 (build 26200, while this was built) Windows gives an icon there the overflow button's rectangle rather than none, and the icon moved where nobody saw it at explorer.exe's cost. `PlacementTests` reads a fake registry and never this machine's; one scratch icon of this code, in the overflow area of one desktop, was watched being held still while this was written, and that is recorded nowhere here. An icon Windows says nothing about - Windows 10, an icon it has not written settings for yet - is left to the rectangle, as before. |
| While the window is open its taskbar button wears the notification-area icon's motion: the window's big icon, which Windows draws the button from, is the icon's own frame for the icon's state for the watcher the window read - the icon's rule as it has it with its popup open, since the window reads the watcher now as the open popup does, and its rhythms and pixels, generated into `Brand.Mark` - while the title bar's small icon keeps every pixel; with no watcher running, or an older one still owning the state, it is the header light's state instead; nothing moves under Reduce motion, Windows' animation setting, High Contrast or battery saver, while the session is locked or disconnected, or with the window hidden, and it moves again when the reason goes; a shutdown another program calls off leaves it following the window; with nothing to move there is no timer, and every icon it makes is destroyed | REAL WINDOWS TESTED, in a real window off-screen | `tests/test_gui_v065_taskbar.py:TaskbarMarkTests` (24 tests), among them `test_the_session_is_windows_own_answer_read_where_windows_writes_it`, `test_every_watcher_the_window_can_see_is_the_tray_icon_s_state_for_it`, `test_the_button_is_the_icon_with_its_popup_closed_except_where_the_open_popup_says_attention`, `test_what_the_window_reads_tells_the_mark_the_tray_icon_s_state_for_that_watcher`, `test_the_frames_are_the_tray_icon_s_own_pixels_at_every_big_icon_size`, `test_there_are_frames_for_each_ico_entry_the_big_icon_is_up_to_300_percent_and_for_no_other_size`, `test_watching_breathes_and_turns_once_on_the_big_icon_and_the_small_icon_never_changes`, `test_each_reason_holds_it_still_and_it_moves_again_when_the_reason_goes`, `test_hundreds_of_frames_hold_the_gdi_and_user_objects_level`, `test_a_shutdown_called_off_leaves_the_button_following_the_window` and `test_closing_stops_the_timer_and_leaves_the_window_its_own_icons_and_nothing_of_the_mark_s`; `tests/test_tray_icon_motion.py:StateTests.test_the_rule_read_from_a_status_light_is_the_tick_s_rule`; `tests/test_brand.py:GeneratedFileTests.test_gui_brand_cs_is_current` | The probe's windows have no taskbar button: that Windows 11 draws the button from the big icon, and reads it again only when the small icon changes, was measured on one machine at 150% while this was built and is recorded nowhere in this repository. Alt+Tab, Windows 10, the *smaller taskbar buttons* setting and ungrouped buttons are unchecked. That the button stops while the session is locked is tested with the probe's own answer standing in for Windows', as battery saver is: what the mark asks Windows (`WTSQuerySessionInformation`) is read alongside it here, with the session's id to show the fields are where it reads them, but nobody has locked a real session to watch the button stop. From 350% the big icon is the `.ico`'s 128 px entry, which has no frames: there the button keeps the window's own icon and neither moves nor turns grey. Each frame makes the taskbar draw the button again, and that work is explorer.exe's, not the window's: measured on one desktop while this was built, 60 s against 60 s at rest, and recorded nowhere here, at about 2 points of one core above rest while it watches and about 3 while it turns, at every rate tried: the taskbar redraws the button no more than about eight times a second, whatever it is sent, so its turn looks coarser than the notification-area icon's. |
| Inside the popup what stands on the card is raised and what holds a value is sunken: a task is a tile made of brand's own recipes on brand's `raised` ground - light's control lift; in dark, the control drop and the dark card's top light - with a shadow small enough for the gap between tiles; the counts sit in one well with a hairline between them; nothing to list and a failed read are said from a well; a pressed button sinks and a resting one stands; a switch that is off sits in a well on its raised tile; High Contrast draws no depth at all; and a count's label is never cut inside a word in any language or scale | UNIT TESTED, and in pixels drawn on Windows | `tests/test_tray_popup_depth.py:RecipeTests` (6 tests), `LayoutTests` (7 tests), `PixelTests` (7 tests), `CountsFitTests.test_no_count_label_is_cut_inside_a_word_in_any_language_or_scale` | Pixels are read back from frames drawn off-screen; nobody has looked at the popup on a real desktop. |
| In the popup a task's switch glides in brand's transition on brand's curve, only once its change is confirmed, turning back from where it is and ending with its row, and nothing moves with motion reduced, in High Contrast or while the popup is hidden; Latin text is set in Windows' UI font, the font the window is drawn in, and Korean, Japanese and Chinese each in its own face | UNIT TESTED, and in a real window off-screen | `tests/test_tray_popup.py:SwitchGlideTests` (4 tests), `WindowsTests.test_a_switch_glides_only_once_its_change_is_confirmed_and_then_the_timer_stops`, `test_a_glide_s_ends_are_the_switch_as_it_is_drawn_at_rest`, `FontTests` (9 tests, incl. `test_every_other_language_is_set_in_windows_own_ui_font_first`, `test_the_popup_asks_windows_for_the_font_the_window_is_drawn_in`) | Nothing measures a glide on screen. |

## 9. The Codex panel and the MCP tools

| Capability | Evidence | What backs it | What is still missing |
| --- | --- | --- | --- |
| The panel is the exact resource the plugin serves, carries everything it renders, fetches nothing, and holds no settings of its own | UNIT TESTED | `tests/test_mcp.py:WidgetTests` (11 tests, incl. `test_the_panel_fetches_nothing`, `test_the_panel_carries_no_settings_of_its_own`, `test_a_preview_seed_is_escaped`) | Never loaded by the real Codex client. |
| The panel shows what is waiting, in the public words, with the state it is in and when it is next looked at | UNIT TESTED | `src/codex_auto_resume/mcpui.py` renders each row's public code and overlays, its attempts and its next check, and the soonest of those beside the count; `tests/test_mcp.py:WidgetTests` (11 tests) cover what the panel is served and `PanelWordingTests` (6) what it says | It is a clock time and not a ticking countdown, because the page is drawn once from one tool result and never refreshed - a number counting down there would go on being wrong convincingly. The live countdown is the window's and the notification icon's. |
| The published panel screenshot is the rendered markup, not a photograph of Codex | REAL WINDOWS TESTED | `assets/screenshot-panel.png` and its Korean sibling, rendered from the served markup (`<panel render:en>` and `<panel render:ko>` in `assets/screenshots.json`), checked by `tests/test_screenshots.py:ManifestTests.test_the_panel_input_is_the_rendered_markup_not_a_file_list` | **Nobody has seen the panel inside Codex.** This is the likeliest place a REAL CODEX VISUALLY TESTED row could exist, and it does not. The picture is the served markup drawn off-screen from sample data, and the suite fails when that markup changes and the picture has not been made again; it is not Codex's frame around the panel, its theme or its webview. |
| The panel speaks a sentence for every refusal it can be handed, in the language it speaks | UNIT TESTED | `tests/test_mcp.py:WidgetTests.test_the_panel_is_served_a_sentence_for_every_refusal_it_may_be_handed`, `test_only_the_refusal_helper_reads_the_english_sentence`; `PanelWordingTests` (6 tests) | - |
| Seventeen tools - v0.6.3 adds the read-only `preview_recovery_message` - each with a schema and an implementation, and the skill lists exactly those | UNIT TESTED | `tests/test_mcp.py:ToolSurfaceTests.test_every_tool_has_a_schema_and_an_implementation`, `test_the_skill_lists_every_tool_and_no_others` | - |
| No tool can send a continuation, and none accepts a conversation by anything but an exact id | UNIT TESTED | `tests/test_mcp.py:ToolSurfaceTests.test_no_tool_can_send_a_continuation`, `test_no_tool_accepts_a_thread_by_anything_but_an_exact_id`; `tests/test_control.py:NotARecoveryEngineTests` (2 tests), `BridgeTests.test_bridge_exposes_no_command_that_sends` | - |
| Pausing runs without a prompt; resuming, budget reset, start watcher and settings changes are marked destructive | UNIT TESTED | `tests/test_mcp_v3.py:ApprovalHintTests` (3 tests); `tests/test_mcp.py:ToolSurfaceTests.test_automation_can_be_reduced_freely_but_only_increased_with_approval`, `test_read_only_tools_are_marked_read_only` | **Whether Codex actually shows the prompt is checked nowhere, and cannot be from here.** The mark is a request, not a lock; nobody has recorded seeing Codex honour it. |
| `update_settings` refuses the advanced settings even from a client that ignores the schema | UNIT TESTED | `tests/test_mcp.py:ToolBehaviourTests.test_an_advanced_setting_is_refused_even_if_the_schema_is_ignored`; `ToolSurfaceTests.test_advanced_settings_are_not_offered_to_a_model`, `test_the_settings_schema_offers_no_unknown_failure_switch` | - |
| Custom message text cannot be set from Codex: `update_settings` does not accept it, and `preview_recovery_message` is read-only and takes no text | UNIT TESTED | `tests/test_mcp.py:ToolSurfaceTests.test_a_model_cannot_write_the_text_sent_into_conversations`, `test_the_preview_tool_is_read_only_and_takes_no_text` | Checked against the server's schema and handlers; the real Codex client has never called either tool. |
| `get_status` does not carry the install path | UNIT TESTED | `tests/test_mcp.py:ToolBehaviourTests.test_status_does_not_carry_the_install_path` | - |
| The transport answers one JSON object per line, survives a malformed line, never answers a notification, and leaks no detail on an internal failure | UNIT TESTED | `tests/test_mcp.py:TransportTests` (15 tests) | Never driven by the real Codex MCP client; negotiation is tested against the test's own requests. |
| The plugin manifest declares no hooks, no environment and no network, and names only this product's own stdio server | UNIT TESTED | `tests/test_plugin.py:ManifestTests` (13 tests) | - |
| The panel's Save and Preview requests are built by the panel's own functions and never carry Custom text, whatever its controls hold; the page has no control that could hold text; what it offers as editable is exactly what the server accepts | UNIT TESTED | `tests/test_mcpui_v063.py:CustomTextBoundaryTests` (3 tests); `RequestShapeTests` (4 tests) run the panel's own request builders in Node against the server's schema | Skipped where Node is absent. Node is not the Codex client: the panel's Preview, and its read-only Custom text, have never been seen inside Codex. |
| The panel's switch for a conversation names that exact conversation and changes only its rows; a refusal, or a reply about another conversation, changes nothing | UNIT TESTED | `tests/test_mcpui_v063.py:ThreadSwitchTests` (4 tests), run in Node | Skipped where Node is absent. |
| Every word the panel's script uses is in the English catalog, including keys it builds from a prefix, and it adds no key the other languages lack; one activity word for the whole product | UNIT TESTED | `tests/test_mcpui_v063.py:CatalogTests` (4 tests), `ActivityTests` (3 tests, run in Node) | - |
| The redesigned panel: reduced motion stops every animation and transition, nothing on the page ticks, the focus ring is for the keyboard only, and every colour token is declared before any theme block | UNIT TESTED | `tests/test_mcpui_v063.py:StyleTests` (8 tests) | Assertions over the served stylesheet. No capture shows the panel moving, or focused from the keyboard. |
| The panel is made of brand's material: its lift is `brand.css_elevation` and resolves to the CSS v0.6.3 wrote, its type and shared sizes are brand's, the primary button brightens by a token rather than a filter, a disabled control says so in muted text at 4.5:1 or better in each of its themes rather than in opacity, and the save card stands on the card ground | UNIT TESTED | `tests/test_mcpui_v063.py:MaterialTests` (5 tests) | Assertions over the served stylesheet; the contrast is worked out from its tokens, counting the 0.7 opacity a browser gives a disabled `select` unless the stylesheet overrides it. Never loaded in Codex. |
| The panel's light is a flat dot of the size it always had, filled in brand's colour for its state, its opacity over the card dimming and its glow spreading on brand's cycle and curve - the glow only while the dot is lit, falling off as brand says with no edge - running that cycle once for a problem, and lit and still with no glow while waiting and with less motion; a watcher that is not running keeps its word and gets a grey light, and amber is only for a watcher that runs | UNIT TESTED | `tests/test_mcpui_v063.py:StatusLightTests` (9 tests) compare the stylesheet's colours, gradient stops and keyframes with `brand.status_fill`, `brand.glow_stops` and `brand.glow`; `HeroLightTests` (2 tests) run the panel's own `renderHero` and `lightFor` in Node | The curves are read from the keyframes, not from a running animation. `HeroLightTests` is skipped where Node is absent. Never seen inside Codex. |
| Under forced colours the panel's lights and master dots are solid system colours with no glow, its lights, dots and switch knobs opt out of forced colours so they stay visible, and no card, button, field or segment keeps a shadow | UNIT TESTED | `tests/test_mcpui_v063.py:StatusLightTests.test_high_contrast_draws_a_solid_system_colour_and_no_glow`; `MaterialTests.test_a_disabled_control_says_so_in_muted_text_not_in_opacity` requires the forced-colours block to fade no disabled control | Assertions over the `@media (forced-colors: active)` block. The select arrow is in that block and no test names it. Forced colours have not been turned on in Codex, and nothing records the panel in a High Contrast theme. |
| Codex can change the theme: `update_settings` offers `theme` with its three choices, changes it through the shared layer and refuses any other value, while the notification-area icon and Reduce motion stay unoffered and are refused even from a client that ignores the schema | UNIT TESTED | `tests/test_mcp.py:ToolSurfaceTests.test_the_theme_is_offered_and_the_windows_only_preferences_are_not`; `ToolBehaviourTests.test_the_theme_is_changed_through_the_shared_layer_and_a_bad_one_is_refused`, `test_a_windows_only_preference_is_refused_even_if_the_schema_is_ignored` | Checked against the server's schema and handlers; the real Codex client has never called it. |
| In the panel a kind of interruption or a notification event is a check box before its label, with no switch role, and what runs is a switch; a box saves like any other choice, and with no host it is disabled and still drawn; each of its four states paints brand's colours in both themes, at brand's size, corners and tick, centred on its label's first line, says disabled in colour rather than by fading, is brand's system colours with no shadow under forced colours, and has the focus ring every control has | UNIT TESTED | `tests/test_mcpui_v064.py:ControlKindTests` (4 tests) run the panel's own script in Node; `CheckBoxStyleTests` (7 tests) resolve the served stylesheet against `brand.check_box` and `brand.check_box_system` | `ControlKindTests` is skipped where Node is absent. The tick is a CSS `clip-path` that Codex's own webview has never drawn; the committed panel pictures are drawn off-screen, and none shows a disabled or focused box. |
| In the panel a switch or a button at the right of a row is pinned to the row's bottom right: Pause or Resume recovery beside the Automatic recovery tile's state and what the last press answered, Show notifications beside the last line of its label, and each waiting conversation's Auto-resume switch beside the line with its next check and attempts. Each comes after its text in the markup, keeps its size and is moved by no CSS `order`; the text beside it wraps, or it drops under the text and stays on the right, and a one-line row is centred as before; an open turn-off question is a line of its own under both; narrow widths, forced colours and reduced motion move none of them; and a select, a number, a chip, a check box and the buttons already at the bottom keep their places | UNIT TESTED | `tests/test_mcpui_v064.py:PinnedControlStyleTests` (4 tests) resolve the served stylesheet's flex rules for each pinned control, its row and its text; `PinnedControlMarkupTests` (3 tests) run the panel's own script in Node and read the order of each row, including a refused pause written under the state beside the button | `PinnedControlMarkupTests` is skipped where Node is absent. The tests hold the flex arrangement and the order of the markup, not where a browser puts each control, and no measurement of that is stored. Never loaded in Codex. |
| The panel's theme: Light and Dark stamp the page's root, *Use system setting* stamps nothing and leaves Codex's own scheme in charge, the stored theme is on the root before the first paint, a saved one applies at once in place and a refused save changes nothing, a page served with a pinned theme keeps it, the Appearance card offers the three choices in the panel's words, and the theme is the only appearance setting the panel may change | UNIT TESTED | `tests/test_mcpui_v064.py:ThemeTests` (7 tests) run the panel's own script in Node; `ServedThemeTests` (2 tests) read the served page | `ThemeTests` is skipped where Node is absent. How Codex treats the stamp, and whether it tells the page its own scheme, has never been seen. |
| The panel speaks a saved Interface language at once: every language's words for the page ship with it, escaped, and the page stays self-contained; a stored language resolves as Python adopts it, System speaks Windows' language, the stored language wins over the one the page was served in, a language the page has no words for keeps the words it has and says the change waits, and a save that changes no language does not redraw | UNIT TESTED | `tests/test_mcpui_v064.py:ServedLanguageTests` (4 tests); `LanguageTests` (7 tests) run the panel's own script in Node, where a Korean save redraws the page in Korean with the keyboard on the new Save | `LanguageTests` is skipped where Node is absent. The page Codex is served grew from about 105 KB to about 205 KB to carry the words, and how Codex takes that, and the redraw, has never been seen. |
| A panel save leaves out the Interface language and the theme while their controls still hold what the page was drawn with, so a change made elsewhere survives a save of something else, and a save that shows either was changed elsewhere redraws the page from what is stored | UNIT TESTED | `tests/test_mcpui_v064.py:ChangedElsewhereTests` (4 tests), in Node | Skipped where Node is absent. Every other setting is still sent from the panel's snapshot, so one changed elsewhere while the panel is open can be overwritten, as before. |
| The panel's drop-downs are a combobox over a hidden select whose list is the panel's own: a card floating over the page, twelve whole rows scrolled by whole rows, its items pills - raised under the pointer, sunken when current - the keyboard's ring every control's; the keys are the window's - F4, Alt and the arrows, Enter, Space, Tab, Escape, and letters finding by the start of a name - a pick is a change the page records and saves, and no native list can open | UNIT TESTED, in Node | `tests/test_mcpui_v065.py:ComboMarkupTests` (4 tests), `ComboKeyboardTests` (13 tests), `ComboPointerTests` (6 tests), `ComboPageTests` (5 tests), `ComboPlacementTests` (4 tests), `ComboStyleTests` (4 tests), `OneListTests` (10 tests) | Skipped where Node is absent. Run against the panel's own script in Node, never inside Codex; no screen reader has read it. |
| In the panel a switch glides its knob and cross-fades its track and a check box fades its fill and mark, every transition brand's time on the one curve and paint only; nothing moves in High Contrast or with less motion; a switch that asks first does not move on the press, glides only once the tool confirms, and stays where it was on a cancel or a refusal | UNIT TESTED | `tests/test_mcpui_v065.py:MotionStyleTests` (6 tests), `ConfirmedSwitchTests` (6 tests) | Read from the stylesheet, and the script run in Node; never seen inside Codex. |
| The panel's tiles - a waiting conversation's row and the master switch's - are raised as the popup's task tiles are, shadow for shadow, declared for each theme from brand; Korean keeps its words whole and Japanese its phrases, and nothing else changes how lines break; the page's root names the language the page speaks | UNIT TESTED | `tests/test_mcpui_v065.py:TileLiftTests` (3 tests), `LineBreakStyleTests` (2 tests), `PageLanguageTests` (3 tests) | Read from the stylesheet and run in Node; never seen inside Codex. |
| The panel's Codex compatibility card draws the summary `open_settings` sends, read-only: folded under what is waiting with the headline on its chip, each part a row with its state in words, each word shown explained, when it was checked and which data is in force by its number, the watcher still acting on what it found at its start said, refreshed data it cannot fully trust said in the window's words, and where the data is refreshed; nothing on it can be pressed, and its script calls no tool that refreshes or imports | UNIT TESTED, in Node | `tests/test_mcpui_compat.py:CardTests` (12 tests), `FromTheServerTests` (2 tests), `NoRefreshTests` (2 tests), `StyleTests` (2 tests) | Skipped where Node is absent. Never seen inside Codex. |
| `get_status` carries the compatibility summary as codes only - status, overall, acting, source, sequence, cache, checked_at and each capability's state and reason - with no Codex version string, path, digest or home in it; no tool refreshes or imports registry data; and no `experimental_*` setting can ever be offered to a model | UNIT TESTED | `tests/test_compat_surfaces.py:McpTests` (4 tests, incl. `test_get_status_carries_the_summary_codes_only`, `test_no_tool_can_refresh_or_import_registry_data`); `tests/test_compat.py:ViewTests.test_what_a_model_may_read_is_codes_only` | - |

## 10. The command line

| Capability | Evidence | What backs it | What is still missing |
| --- | --- | --- | --- |
| Fourteen of the fifteen commands, end to end: install, enable, status, disable, pending, cancel, logs, run, stop, uninstall, doctor, activate, diagnostics, downgrade-state (`compat` has its own row below) | UNIT TESTED | `tests/test_cli.py:CliTests` (9 tests, incl. `test_install_enable_status_disable_cycle`, `test_run_once_without_engine_fails_closed`, `test_uninstall_removes_only_owned_files`, `test_stop_without_watcher`) | - |
| `downgrade-state --to 2` produces a file the tagged v0.5.7 code opens, with every row kept | UNIT TESTED | `tests/test_downgrade.py:DowngradeTests.test_the_tagged_v0_5_7_store_opens_the_result_with_every_row` loads the store module from that git tag and opens the result with it; `test_upgrading_again_restores_a_working_v3_state` | Skipped where the tag is not in the checkout - a failure on CI, a silent skip locally. It tests the old *store module*, not the old release running. |
| `downgrade-state` refuses while the watcher runs | UNIT TESTED | `tests/test_downgrade.py:DowngradeCommandTests.test_it_refuses_while_the_watcher_runs` | - |
| `doctor` reports the protocol registration and the engine it found | UNIT TESTED | `tests/test_cli.py:DoctorProtocolCheckTests` (3 tests), `DiscoveryTests.test_discovery_requires_single_compatible_binary` | Against a fake registry. |
| The watcher loop survives a transient adapter failure and a store read failure | UNIT TESTED | `tests/test_cli.py:WatcherLoopTests` (2 tests) | - |
| Log rendering is from a fixed message table and prints Windows local time | UNIT TESTED | `tests/test_cli.py:LogbookTests` (2 tests) | - |
| The entry point runs with no `PYTHONPATH` from an unrelated directory and puts its own package first | UNIT TESTED | `tests/test_cli.py:EntryPointTests` (2 tests) | - |
| `compat` reads the watcher's report, `--live` checks now and writes nothing, `--json` prints the view, and `--import FILE` validates a document and keeps it only if it passes; `status` and `doctor` carry the compatibility line, and they and the start-up log agree with the registry data in force | UNIT TESTED | `tests/test_compat_surfaces.py:CommandLineTests` (7 tests) | Against the registry's hermetic fixture: its own fake Codex and its own home. |
| Every state line in the log carries the reason it was given, once, through the engine's own adapter; what counts as a state is derived from the state machine, so it cannot miss one again; a reason that is not a static code is not printed | UNIT TESTED | `tests/test_logbook.py:StateVocabularyTests` (2 tests), `ReasonTests` (6 tests), `PrivacyTests` (4 tests) | - |

## 11. Windows notifications

| Capability | Evidence | What backs it | What is still missing |
| --- | --- | --- | --- |
| A toast is raised under this product's own identity rather than PowerShell's | UNIT TESTED | `tests/test_notify.py:NotificationIdentityTests` (4 tests) | No capture of a real toast is committed. The one the README showed through v0.6.4 had gone stale with nothing noticing - it predated Open Dashboard and no manifest pinned it - and was removed; the notification the README pictures now is the product's card, which is not the toast. |
| The toast is drawn only because a Start Menu shortcut carries the same AppUserModelID | REAL WINDOWS TESTED | `tests/test_surface_properties.py:ShortcutTests` (5 tests) run the real `shortcut.install` with `APPDATA` pointed at a temporary directory, then read the target and description back through the shell's own shortcut object and find the AppUserModelID in the file's bytes, and check that the notifier sends under that same id; `src/codex_auto_resume/shortcut.py` records the measurement on Windows 11 - without the shortcut the platform still accepts the toast and logs it delivered, and simply never draws it | The id is read out of the file rather than through `IPropertyStore`, and no toast has been raised and seen. |
| The shortcut's own values travel beside its script, never inside it | UNIT TESTED | `tests/test_pwsh.py:NoValueInScriptTests.test_the_shortcut_values_travel_out_of_band` drives `shortcut.install` with `subprocess.run` patched, decodes the `-EncodedCommand` that was handed to PowerShell and requires it to be `shortcut._MAKER` unchanged, with an install path carrying an apostrophe arriving in the environment; `test_every_pwsh_run_call_passes_a_module_constant` names `shortcut.py` as one of the two callers | No PowerShell runs and no file is written in that test. **`shortcut.uninstall` is exercised nowhere**: that it removes only our own name in our own folder, and refuses a symlink, is reviewed rather than tested. |
| A missing identity registration never stops a notification | UNIT TESTED | `tests/test_notify.py:NotificationIdentityTests.test_an_unregistered_identity_still_sends`; the class docstring records that delivery was measured working unregistered on Windows 11 | That measurement is described, not stored as an artifact. |
| No conversation content reaches a toast; at most three lines; the text is XML-escaped | UNIT TESTED | `tests/test_notify.py:ToastPayloadTests` (13 tests, incl. `test_no_conversation_content_is_disclosed`, `test_never_more_than_three_lines`, `test_the_embedded_document_is_still_valid_xml`, `test_a_quote_in_the_text_cannot_break_out_of_the_script`, `test_the_escaping_itself_has_not_moved`) | - |
| No value ever reaches PowerShell as script text | REAL WINDOWS TESTED | `tests/test_pwsh.py:RealInterpreterTests` (2 tests) run the real Windows PowerShell with hostile values; `NoValueInScriptTests` (5 tests) and `EnvironmentTests` (4 tests) pin the shape | Skipped where PowerShell is absent. |
| A failing or missing PowerShell is not an error | UNIT TESTED | `tests/test_notify.py:ToastPayloadTests.test_a_failing_or_missing_powershell_is_not_an_error` | - |
| The Cancel button only ever cancels, and a hostile or malformed URI never reaches the store | UNIT TESTED | `tests/test_notify.py:CancelUriTests` (4 tests), `ActivationTests` (3 tests) | The path from a real toast click through the real protocol handler has never been exercised. |
| Both interruption notifications carry Open Dashboard, which can only open one of the window's own pages - the Pending page - and changes nothing | IMPLEMENTED | `src/codex_auto_resume/notify.py` (`open_uri`, `parse_open_uri`) builds and reads a URI naming a page from a closed list, and `src/codex_auto_resume/cli.py` (`cmd_activate`) opens that page and does nothing else | **No test covers it.** Nothing in `tests/test_notify.py` builds, parses or activates the open URI, and no click on a real toast has been recorded. |
| A transient interruption's notification names the kind of interruption | IMPLEMENTED, with the line limit tested | `src/codex_auto_resume/notify.py` (`scheduled`) puts the reason registry's label - never the error text - in front of the sentence; `tests/test_notify.py:ToastPayloadTests.test_the_reason_and_the_uuid_both_survive_the_line_limit` requires that line to survive the three-line limit | That test looks for the sentence, not the label, so a notification that lost the label would still pass it. |
| Every lifecycle event has its own switch, and a master switch silences all of them | UNIT TESTED | `tests/test_notify.py:EngineNotificationTests.test_every_lifecycle_event_maps_to_a_notification_setting`, `NotificationSettingTests.test_messages_exist_in_every_language`; `tests/test_settings.py:PolicyTests.test_master_notification_switch_silences_every_event`, `test_individual_notification_events_are_independent` | - |
| An interruption is announced once however often it is seen, and a failing notification never blocks a recovery | INTEGRATION TESTED | `tests/test_notify.py:EngineNotificationTests.test_an_interruption_is_announced_once_however_often_it_is_seen`, `test_a_failing_notification_never_blocks_a_resume` | - |
| A child registered already stopped is announced as stopped only | INTEGRATION TESTED | `tests/test_outcomes.py:T48NotificationTests.test_T48_a_child_registered_exhausted_is_announced_as_stopped_only`, `test_T48_children_registered_cancelled_or_superseded_are_announced_as_stopped_only` | - |
| A notification is shown exactly once, by exactly one route: the product's card only when every answer is exactly the one that allows it - the setting on, the icon up, Windows accepting notifications, Do not disturb and Focus off, this app's notifications on, no screen reader, not remote, not locked - and today's toast otherwise, including when a probe breaks or cannot answer; the card holds for Windows' own duration when that is longer | UNIT TESTED | `tests/test_notice_card.py:PolicyTests` (10 tests), `RouteTests` (4 tests) | The probes are called on this machine only for the vocabulary of their answers; nothing switches Do not disturb, a full-screen app or a screen reader on to watch the route change. |
| The toast route is byte for byte what v0.6.4 raised, for every event, language and case, whatever the watcher's notifier calls; the card's lines are the toast's lines, its buttons the toast's buttons and its chip the reason's catalog label, it carries no field it was not built from, and only a detected interruption offers anything | UNIT TESTED | `tests/test_notice_card.py:CharacterizationTests` (6 tests) against goldens captured from `git archive v0.6.4`, `NoticeTests` (8 tests), `PrivacyTests` (2 tests) | - |
| Once the card has been on screen whole, the same toast is raised silently, its banner turned off by a value rather than by script text, so the history is what it always was; a card that could not be drawn, or failed before it was seen, becomes the toast; one replaced or clicked before it was whole is in the history; and every notice the stack held ends exactly once | REAL WINDOWS TESTED, for the card's own windows | `tests/test_notice_card.py:SilentCopyTests` (3 tests); `WindowsTests.test_the_history_copy_follows_only_once_the_card_has_been_seen_whole`, `test_a_notice_that_cannot_be_drawn_goes_back_to_the_toast`, `test_a_card_whose_frames_fail_before_it_was_seen_becomes_the_toast`, `test_a_card_replaced_or_clicked_before_it_was_whole_is_in_the_history`, `test_every_notice_the_stack_had_ends_once_when_it_goes_away` | Skipped off Windows. Nobody has looked in the notification center after a real card. |
| The card is two layered windows beside the notification area that never take the focus: at most three stack, one per conversation, the newest in the corner, inside the work area for every taskbar edge and scale, and a short screen keeps only those that fit; it rises, fades, grows and deepens on brand's ease-out, holds while pointed at and a little after, and with motion reduced appears and goes with no frame between; over the wallpaper its shadow only darkens; it is opaque inside its rounded corners; its light is the still light, lit with no glow; High Contrast is system colours with no shadow; and it leaves nothing behind | REAL WINDOWS TESTED, off any person's view | `tests/test_notice_card.py:WindowsTests` (18 tests, incl. `test_a_posted_notice_becomes_two_windows_that_never_take_the_focus`, `test_three_at_most_and_one_card_per_conversation`, `test_the_cards_light_is_lit_and_still_with_no_glow`, `test_high_contrast_has_no_shadow_and_system_colours`); `MotionTests` (10 tests), `StackTests` (6 tests), `ShadowTests` (6 tests), `LayoutTests` (3 tests) | Skipped off Windows. Nobody has watched a card arrive on a real desktop, over a real wallpaper, or on a second monitor. |
| A card's button does in process only what the toast's button does: an open URI opens one of the window's pages, a cancel URI cancels exactly that interruption with the toast's actor, a malformed one never reaches the control layer, and every press leaves one line in the log; the card imports nothing that can act, names nothing that sends, asks the control layer for nothing, drives no other window, opens no connection and raises no PowerShell of its own, and every colour in it is a brand token | UNIT TESTED | `tests/test_notice_card.py:ActivateTests` (6 tests), `SafetyTests` (8 tests), `InboxTests` (6 tests) | No real click on a real card is recorded. |
| The setting that turns the card off is offered exactly while something draws the card, in the window only, and a model cannot turn it off | UNIT TESTED | `tests/test_notice_card.py:SettingTests` (2 tests) | - |

## 12. Platform pieces

| Capability | Evidence | What backs it | What is still missing |
| --- | --- | --- | --- |
| A single-instance mutex refuses a second watcher | REAL WINDOWS TESTED | `tests/test_engine.py:SingleInstanceTests.test_16_duplicate_watcher_process_is_refused` holds a real named mutex in a real second process and requires the App to exit busy | Skipped off Windows. The live variant, `tests/test_integration_live.py:LiveReadOnlyTests.test_single_instance_mutex_and_stop_event_roundtrip`, needs `CODEX_AR_LIVE=1` and nothing records a run. |
| The stop event wakes a waiter, and stopping is always an ask | REAL WINDOWS TESTED | `tests/test_engine.py:SingleInstanceTests.test_stop_event_wakes_waiter` uses real named events; `tests/test_control.py:StopWatcherTests.test_the_stop_is_asked_once_and_nothing_is_ever_killed` | - |
| A mutex or stop event planted by a lower-integrity process is refused, and the status reads unknown rather than running | REAL WINDOWS TESTED | `tests/test_named_objects.py:SquattedObjectTests` (5 tests) create real named objects carrying a Low integrity label and require the watcher to refuse them | Skipped off Windows. It does **not** cover a lower-integrity process opening the running watcher's own Medium-labelled mutex - SECURITY.md says so, and nothing tests it because nothing defends it. |
| The wake event lets Retry now shorten a wait | REAL WINDOWS TESTED | `tests/test_surface_properties.py:WakeEventTests` (4 tests) hold a real named event in a second process waiting two minutes on it, signal it from this one, and require the wait to end on that event in well under thirty seconds; also that signalling with nobody waiting answers false, that one state directory's event does not reach another's, and that the fallback with no event still sleeps rather than spinning; the fallback where it is unavailable is also covered by `tests/test_cli.py:WatcherLoopTests` | Both processes are in one logon session. |
| The home lock keeps two processes out of one state directory | REAL WINDOWS TESTED | `tests/test_surface_properties.py:HomeLockTests` (5 tests) take the real lock and require the second holder to be refused with `home_lock_busy` - in this process and in a second one - and require it free again once the holder is gone, unrelated homes independent, and one home spelled two ways to be one lock; `home_lock_unavailable` is a first-class refusal reason in `src/codex_auto_resume/machine.py` | Both processes are in one logon session; the cross-session case the lock also exists for has not been run. |
| Sign-in start writes one value under the current user's `Run` key, is idempotent, pins the effective home, and is removable | UNIT TESTED | `tests/test_cli.py:StartupTests` (4 tests); `tests/test_plugin.py:AutostartOwnershipTests` (5 tests), `BridgeTests.test_setup_refuses_when_another_installation_owns_autostart`, `test_no_startup_never_writes_the_registry` | Against `FakeWinreg`. **No test writes a real `Run` value**, and nothing in the repository records the watcher actually starting at a Windows sign-in. A captured `logs/launcher.log` from a real sign-in would be that artifact. |
| The registered command survives a path with a space, a trailing backslash, an embedded quote, and a Unicode path | REAL WINDOWS TESTED | `tests/test_cli.py:CommandQuotingTests` (6 tests) compare against `CommandLineToArgvW` itself on Windows; `tests/test_installer_ownership.py:ArgumentQuotingTests` (6 tests) | Skipped off Windows. |
| Short-path and case spellings of the home resolve to the same installation | UNIT TESTED | `tests/test_plugin.py:ShortPathOwnershipTests` (7 tests) | - |
| The `codex-auto-resume:` protocol handler is registered per user, carries only the activate verb, and is removed only if it is ours | UNIT TESTED | `tests/test_notify.py:ProtocolRegistrationTests` (4 tests); `tests/test_cli.py:DoctorProtocolCheckTests` | Against a fake registry. **No real protocol registration, and no recorded activation from a real toast button.** Since v0.6.3 the same verb also carries the Open Dashboard request, which no test covers (§11). |
| The watcher launcher calls system binaries by absolute path and resolves nothing through the current directory | UNIT TESTED | `tests/test_upgrade_handover.py:LauncherPathTests` (3 tests) | - |

## 13. Install, upgrade, repair, uninstall

| Capability | Evidence | What backs it | What is still missing |
| --- | --- | --- | --- |
| The installer parses as PowerShell | REAL WINDOWS TESTED | `tests/test_installer.py:ParseTests.test_the_installer_parses_as_powershell` runs the real PowerShell parser over `install/install.ps1` | Parsing is not running. Nothing in the suite executes the installer end to end. |
| It needs no administrator rights, creates no service or scheduled task, writes no machine-wide registry, downloads nothing at install time, and uses the bundled runtime | UNIT TESTED | `tests/test_installer.py:SafetyTests` (12 tests) | Source assertions over `install/install.ps1`. A real install is never performed. |
| Program directories are moved aside rather than deleted, and a failure at either step puts the installation back | UNIT TESTED | `tests/test_installer.py:InPlaceUpgradeTests` (7 tests) | - |
| The move-aside journal is written whole before the first move, read before anything is swept, and deleted only when both trees are in place | UNIT TESTED | `tests/test_installer.py:CrashDuringTheCopyTests` (10 tests), `JournalRecoveryTests` (11 tests, incl. `test_a_tree_left_aside_by_an_interrupted_run_is_put_back`, `test_a_path_outside_the_installation_is_never_moved`, `test_a_journal_that_cannot_be_read_stops_before_the_sweep`) | No power cut has been simulated on a real installation; the crash points are chosen by the test. |
| Only the settings window and its icon are copied to the payload root, by name, and an archive carrying anything else is refused | UNIT TESTED | `tests/test_installer.py:PayloadRootTests` (6 tests), `ArchiveContentsTests` (5 tests) | - |
| A directory is treated as an installation only on proof of ownership, and nothing outside the verified root is ever removed | UNIT TESTED | `tests/test_installer_ownership.py:InstallHomeProvenanceTests` (9), `PathConfinementTests` (7), `RemoveOwnedItemTests` (3), `InstallHomeClaimTests` (8), `InstallerDeletionRoutingTests` (4), `ProcessOwnershipTests` (2) | - |
| An upgrade runs setup with `--keep-state`, so it never re-enables recovery and never re-adds a removed sign-in start | UNIT TESTED | `tests/test_installer.py:UpgradeKeepsTheOwnersChoiceTests` (6 tests); `tests/test_plugin.py:BridgeTests.test_keep_state_neither_resumes_nor_adds_an_autostart`, `test_keep_state_repairs_an_autostart_that_is_already_ours`, `test_without_keep_state_setup_still_enables_and_registers` | Never observed across a real version-to-version upgrade. |
| The watcher is handed over before the files are replaced, through the stop event, with a bounded wait, and one left running is reported | UNIT TESTED | `tests/test_upgrade_handover.py:UpgradeHandoverTests` (6 tests) | - |
| The sign-in launcher accepts only this product's own marketplace, and otherwise starts nothing | UNIT TESTED | `tests/test_plugin.py:LauncherResolutionTests` (8 tests, incl. `test_a_same_named_plugin_from_another_marketplace_is_never_run`, `test_only_another_marketplace_means_nothing_starts`); `tests/test_convergence.py:EngineResolutionTests` (4 tests) | - |
| The bootstrap verifies before it executes, accepts only GitHub hosts, and leaves nothing behind on failure | UNIT TESTED | `tests/test_convergence.py:BootstrapTests` (14 tests), `ReleaseManifestTests` (6 tests), `HostPortabilityTests` (3 tests) | Source assertions over `scripts/bootstrap.ps1`. No download is performed in the suite. |
| The version an ordinary run fetches cannot be supplied; only `-Update` moves it, and only to a version resolved under this exact repository | REAL WINDOWS TESTED | `tests/test_update_check.py:ResolverTests` (9 tests) lift the shipped resolver out of `scripts/bootstrap.ps1` with the PowerShell parser and run it against nineteen final URLs with the network replaced: a fork, another repository of the same owner, an owner whose name merely starts with this one, a host allowed for downloads but not for releases, plain HTTP, a tag that is not a version, a traversal, and a response that will not say where it came from. Four of those nineteen are accepted and the rest refused; `tests/test_convergence.py:BootstrapTests` (5 of 14) pin the shape | Every case is a stub. The one real request this makes is `-CheckOnly` against github.com, which is not in the suite and has to be run by hand; it was, on the day v0.6.0 was published, and it resolved the newest release through the redirect and reported it against the version installed on that machine, having downloaded nothing. |
| Asking whether there is an update transfers no page, sends nothing about you, and never happens on its own | UNIT TESTED | `tests/test_update_check.py:ResolverTests.test_it_asks_for_no_body_and_asks_only_the_constant` requires one `HEAD` to the constant URL; `tests/test_convergence.py:BootstrapTests.test_the_update_check_never_parses_what_the_server_sends`, `test_nothing_checks_for_an_update_unless_asked`; `tests/test_gui_update.py:ButtonTests.test_nothing_asks_without_being_asked` requires the window's only caller to be the button | What GitHub records for an anonymous request - an address, a time, a user agent - is GitHub's, and is stated in `PRIVACY.md` rather than checked here. |
| Four answers, four exit codes, and "could not ask" never becomes "up to date" | REAL WINDOWS TESTED | `tests/test_gui_update.py:BootstrapReadingTests` (4 tests) call the compiled window's own `RunBootstrap` against thirteen real child processes, including every combination where the exit code and the printed line disagree, which must be believed as neither; `tests/test_update_check.py:EndToEndTests` (5 tests) run the whole script against a port nothing is listening on and require exit 12, `update: unavailable`, and nothing installed | The "newer local build" answer is exercised through stubs and through the window, never against github.com. |
| An update installs through the same installer, and whether the watcher restarted is checked by its start time rather than by a version | REAL WINDOWS TESTED | `scripts/bootstrap.ps1` sets the target version and falls through to the ordinary download, checksum, contents and version checks and then `install/install.ps1`, whose `--keep-state` is tested by `tests/test_installer.py:UpgradeKeepsTheOwnersChoiceTests` (6 tests); `gui/Dashboard.cs` (`OfferUpdate`, `AfterUpdate`, `WatcherIdentity`), pinned by `tests/test_gui_update.py:ButtonTests.test_the_watcher_identity_is_the_start_time_not_the_version` | An upgrade has now been performed, once: a real v0.5.6 installation on a machine with a real Codex present was taken to v0.6.0 by an ordinary setup run, which matched the pinned digest rather than the sidecar, kept the owner's settings and pending work, and left the installed copy answering `current 0.6.0`. [`docs/evidence/real-upgrade-2026-09-13.json`](evidence/real-upgrade-2026-09-13.json) records it. A second upgrade, to v0.6.2, was run with a watcher already running, and the handover was observed the way the check makes it: the installer stopped the old watcher, the new one came up under a different process and a later start time, and the settings, the four interruption records and the journal were all still there afterwards. Still no interruption happened during either, so nothing here is evidence about recovery. `docs/LIVE_ACCEPTANCE.md` is still where the rest belongs. |
| An installation is never replaced by an older one unless `-Force` asks | REAL WINDOWS TESTED | `tests/test_update_check.py:NeverGoesBackwardsTests` (5 tests) run the whole script against installations laid out on disk at a newer version, the same version, an older one and one whose version cannot be read, with no host reachable, and require it to converge rather than download in exactly the first two cases - and to download when `-Force` asks | The installations are directories shaped the way `Get-InstalledVersion` reads them, not real ones. |
| The installer takes a lock before it touches anything, and an abandoned lock is taken rather than treated as contention | UNIT TESTED | `tests/test_convergence.py:InstallerLockTests` (3 tests) | - |
| Uninstall removes only what it created, aborts when the watcher state is unknown, and keeps state unless purging is asked for | UNIT TESTED | `tests/test_cli.py:UninstallSafetyTests` (6 tests); `tests/test_installer.py:UninstallKeepsStateTests` (5), `UninstallStatePreservationTests` (3) | Never run against a real installation. |
| Repair from the window | IMPLEMENTED | See §7 | Its five outcomes are unverified. |

## 14. The state store

| Capability | Evidence | What backs it | What is still missing |
| --- | --- | --- | --- |
| Schema 3 is created and validated; a corrupt file, a newer schema or an unknown record state fails closed and is never silently reset | UNIT TESTED | `tests/test_store.py:StoreTests.test_corrupt_database_fails_without_reset`, `test_newer_schema_and_unknown_record_state_fail_closed`, `test_existing_empty_state_is_never_silently_reset`, `test_invalid_settings_fail_closed` | - |
| Migration 1 to 2 to 3 happens in one transaction, only under the watcher's mutex, with a forensic copy of the old file taken first | UNIT TESTED | `tests/test_store.py:MigrationTests` (5 tests, incl. `test_T37_open_without_migrate_is_refused_and_touches_nothing`, `test_T37_migration_preserves_every_row_and_backfills_the_chain`, `test_T19_T37_a_crash_during_migration_leaves_the_old_schema_or_the_new`) | The migration also ran once on the machine this document was written on, whose working tree holds a schema-2 forensic copy, `config/state.v2-backup-20260911T190545Z.sqlite`, beside a live schema-3 `config/state.sqlite` carrying the `events` and `watcher_status` tables. **No reader can confirm that.** `config/` is deliberately untracked (`tests/test_repo_hygiene.py:RepositoryHygieneTests.test_no_copied_runtime_state_is_tracked`), so a clone of this repository holds no trace of the run, and this sentence is the whole of the record. |
| Until the state is migrated, only the actions that reduce automation work | UNIT TESTED | `tests/test_control_v3.py:UpgradeWindowTests.test_only_what_reduces_automation_works_until_the_old_watcher_stops`, `test_a_newer_state_is_never_called_damage` | Skipped off Windows, and skipped where the v0.5.7 tag is absent from the checkout. |
| The transition table allows exactly the plain moves; the moves reserved for dedicated operations are refused to `update` | UNIT TESTED | `tests/test_store.py:TransitionTableTests` (8 tests, incl. `test_T12_nothing_possibly_sent_returns_to_waiting_by_update`, `test_T12_validator_rejects_observing_without_its_proof`) | - |
| A crash mid-write leaves the record whole or absent | UNIT TESTED | `tests/test_store.py:CrashInjectionTests` (2 tests) | The crash is injected at chosen points, not by killing a process. |
| The journal stores codes, ids, counters and times and nothing else; it is bounded at 5,000 entries and 90 days; it never drops an entry of a running recovery; event ids are never reused | UNIT TESTED | `tests/test_store.py:JournalTests` (8 tests, incl. `test_T44_pruning_keeps_the_story_of_every_record_still_running`, `test_T44_event_ids_are_never_reused`, `test_T39_a_foreign_journal_row_never_fails_an_open_or_a_read`) | - |
| Re-entering the same wait journals once; two writers of one transition journal once; a refused change journals nothing | UNIT TESTED | `tests/test_store.py:JournalTests.test_T44_re_entering_the_same_wait_journals_once`, `test_T44_two_writers_of_the_same_transition_journal_once`, `test_T44_a_refused_change_journals_nothing` | - |
| Nothing reads the journal back to decide anything | UNIT TESTED | `tests/test_surface_properties.py:JournalIsWriteOnlyTests` (3 tests) require the engine, the app, the source and the failure classifier never to call `events()`, require the set of modules that do to be exactly the timeline and the diagnostics export, and require the control layer's one call to be inside `timeline` | A source-shape check: it holds the shape the reasoning depends on and would pass code that is wrong in a new way. |
| Statistics: buckets, medians, a rate only above five, one final outcome per record, hidden rows included | UNIT TESTED | `tests/test_store.py:StatisticsTests` (4 tests) | - |
| Settings round-trip through one validator; a corrupt or oversized file reads as defaults; no temporary file is left behind; a continuation setting this version does not know reads as its default | UNIT TESTED | `tests/test_settings.py:PersistenceTests` (10 tests), `CoerceTests` (6), `ValidateUpdateTests` (5), `DescribeTests` (6); `tests/test_continuation.py:SettingsResolutionTests.test_a_stored_value_this_version_does_not_know_is_the_default_not_a_crash` | - |
| Three interfaces write the same file through the same validator - except Custom message text, which only the Dashboard writes | UNIT TESTED | `tests/test_control.py:SettingsSurfaceTests` (9 tests), `BridgeTests.test_a_value_written_by_the_bridge_is_what_the_control_layer_reads`; `tests/test_mcp.py:ToolSurfaceTests.test_the_settings_schema_is_generated_from_the_shared_fields`, `test_published_bounds_match_the_validator`, `test_a_model_cannot_write_the_text_sent_into_conversations` | - |
| Theme is a setting like the others: *system*, *light* or *dark*, *system* by default and first in the Appearance group; a value written as one of those is kept, anything else is refused on write and read as *system*, older and hand-edited files included; and it round-trips through the control layer and the bridge the window uses, and goes back to *system* with the defaults | UNIT TESTED | `tests/test_settings.py:ThemeTests` (6 tests); `tests/test_control.py:SettingsSurfaceTests.test_the_theme_round_trips_and_a_bad_one_is_refused_by_name`, `BridgeTests.test_the_theme_round_trips_through_the_bridge_the_window_uses` | What *system* means is each surface's to work out, and is in §7, §8 and §9. |
| A settings update is refused when a value is not of the type the schema publishes, whatever it equals - both the values whose coerced result equals what was supplied and those whose does not - while every default is still a valid write, an integer is still a number, clearing works exactly where it did, and reading a file still falls back instead of refusing | UNIT TESTED | `tests/test_settings.py:WrongTypeTests` (7 tests) | - |

## 15. Privacy and safety properties

| Capability | Evidence | What backs it | What is still missing |
| --- | --- | --- | --- |
| The recovery runtime imports no networking module; exactly one shipped file reaches the network | UNIT TESTED | `tests/test_privacy_claims.py:CodePropertyTests.test_the_recovery_runtime_imports_no_networking_module`, `test_exactly_one_shipped_file_reaches_the_network` | Covers `src/*.py` and `scripts/*.py`. **`gui/*.cs` is not covered.** |
| There is no analytics or telemetry endpoint, and the runtime disables Codex's own telemetry when it drives it | UNIT TESTED | `tests/test_privacy_claims.py:CodePropertyTests.test_there_is_no_analytics_or_telemetry_endpoint`, `test_the_runtime_disables_codex_own_telemetry_when_it_drives_it`; `tests/test_windows.py:BackendTests.test_queue_argv_exact_id_shell_disabled_and_no_telemetry` | That Codex honours the flag is not checked here, and cannot be. |
| Every history query is bounded by the marker; foreign rows are counted, never read | UNIT TESTED | `tests/test_source.py:StaticQueryTests.test_T11_content_is_marker_bounded_and_foreign_rows_are_counts` parses `src/codex_auto_resume/source.py`, takes every SQL string literal out of it and matches the marker bound with a regular expression; `test_T11_checker_catches_an_unbounded_query` proves the checker itself catches a bound that sits only in a sub-query | **The check reads the source. It opens no database and runs no query**, so a query that is wrong in a new way can pass it: a bound the regular expression happens to match, content reached through a join or a table the checker does not model, or SQL assembled from pieces rather than written as one literal. Only `source.py` is parsed, and nothing observes what a query actually returns. |
| No prompt, reply or error text reaches the log, the journal or a reply | INTEGRATION TESTED | `tests/test_engine.py:EngineScenarioTests.test_log_entries_never_contain_prompt_text`; `tests/test_failures.py:DetectionGateTests.test_no_error_text_survives_detection`; `tests/test_control_v3.py:HistoryAndStatisticsTests.test_the_timeline_is_content_free`; `tests/test_control.py:StopWatcherTests.test_every_reply_is_content_free` | `errors.log` deliberately carries exception messages this product did not write. That is documented in PRIVACY.md, and redacted rather than filtered in a diagnostics bundle. |
| A label shown to a person is bounded and never multiline, and prompt-bearing columns are never read to build one | UNIT TESTED | `tests/test_failures.py:DisplayLabelTests` (2 tests), `IdentitySourceTests` (8 tests, incl. `test_prompt_bearing_columns_are_never_read`, `test_extended_length_paths_do_not_leak_the_prefix`) | - |
| Diagnostics: nothing identifying survives, aliases hold inside one file and differ between files, a Custom message is recorded as set and never quoted, and it never overwrites | UNIT TESTED | `tests/test_diagnostics.py:DiagnosticsTests` (8 tests, incl. `test_a_custom_message_is_recorded_as_set_and_never_quoted`) | Run against a constructed log, not against a bundle from a real incident. |
| No shell anywhere, and no value ever reaches a script as text | REAL WINDOWS TESTED | `tests/test_pwsh.py:RealInterpreterTests` (2 tests, real PowerShell), `NoValueInScriptTests` (5 tests), `EnvironmentTests` (4 tests) | - |
| Owned paths are confined; a junction that escapes the home is refused | UNIT TESTED | `tests/test_cli.py:ConfinementTests` (3 tests); `tests/test_installer_ownership.py:PathConfinementTests` (7 tests) | - |
| No GUI automation, no mouse or keyboard simulation, no OCR, no screen scraping, no accessibility-API driving | UNIT TESTED | `tests/test_surface_properties.py:NoGuiAutomationTests` (2 tests) refuse `SendInput`, `keybd_event`, `mouse_event`, `SetCursorPos`, SendKeys, UI Automation, OCR and screen capture in every tracked file under `gui/`, `src/`, `scripts/`, `install/` and `skills/`, and require the one screen capture in the repository - `build/capture_window.ps1`, which makes the README's pictures - to stay out of the release payload | A deny list, so a technique nobody thought of is not refused. The only `user32` call in the product is `SetForegroundWindow` in `src/codex_auto_resume/tray.py`, bringing its own window forward. |
| The Korean documents are current, and make no claim the English ones do not; the roadmap marks which releases are out and which is in development as the manifest says, and the Korean roadmap marks the same | UNIT TESTED | `tests/test_korean.py:MappingTests` (6 tests, incl. `test_no_translation_is_stale`), `ClaimTests` (7 tests), `GeneratorTests` (6 tests), `RoadmapTests` (3 tests) | - |
| Nothing in the repository names a real home directory or carries a non-synthetic UUID | UNIT TESTED | `tests/test_repo_hygiene.py:RepositoryHygieneTests` (5 tests) | - |

## 16. The build and the release

| Capability | Evidence | What backs it | What is still missing |
| --- | --- | --- | --- |
| Two builds of one source differ in exactly two PE fields, and normalising makes the executables byte-identical; a long string `switch`, which makes the compiler name a class at random, is refused rather than shipped | REAL WINDOWS TESTED | `tests/test_reproducible.py:RealCompilerTests` (7 tests) build real executables with the in-box C# compiler and compare real PE bytes; `RandomlyNamedClassTests` (2 tests) compile a program whose string `switch` produces the randomly named class, check the normaliser refuses it, and hold the window's source to having no such `switch` | Skipped where the compiler is absent. No longer one machine only: the archive GitHub's runner published for v0.6.0 and a rebuild of that tag on the machine this document was written on came back byte-identical, whole ZIP and all, and the run log says both ran the same in-box compiler build. What has still never been compared is two *different* compiler builds. |
| The normalised program still runs, and the replacement MVID depends on the module's content | REAL WINDOWS TESTED | `tests/test_reproducible.py:RealCompilerTests.test_the_normalised_program_still_runs`, `test_the_mvid_depends_on_the_content`, `test_the_timestamp_is_the_fixed_value` | - |
| The release build normalises every executable and proves reproducibility by building twice | UNIT TESTED | `tests/test_reproducible.py:BuildScriptTests` (3 tests) | Source assertions over `build/make_gui.ps1` and the workflow; no release run is replayed. |
| Both executables carry a version resource generated from the plugin manifest | REAL WINDOWS TESTED | `tests/test_reproducible.py:VersionResourceTests` (4 tests) run `build/make_gui.ps1` and read the resource back off the two built files with the call behind Explorer's Properties, Details; `BuildScriptTests.test_the_executables_carry_a_version_resource_from_the_manifest` covers the script itself | Skipped where the in-box compiler is absent. Nobody has opened the tab by hand. The published v0.6.0 executables have now been read back the same way, out of the downloaded archive: both report `0.6.0.0`, `0.6.0` and `Codex Auto Resume`. |
| v0.6.0 is the first release that can be rebuilt from its tag and compared | PUBLISHED | The published archive was rebuilt from a fresh clone of `v0.6.0` on a different machine from the one that published it, and the whole ZIP came back byte-identical - `791e9248…faf8fbc3` both times, and both executables matched the digests the release run printed for them. [`docs/evidence/reproducible-build-2026-09-13.json`](evidence/reproducible-build-2026-09-13.json) records both toolchains; `docs/VERIFY.md` describes the experiment | **Cross-machine, same compiler build, byte-identical - measured once.** The release run's log names the runner's compiler, and it is the same build as the rebuilding machine's, so the comparison did not vary the one variable `docs/VERIFY.md` says the result depends on. It did vary the machine and the Windows build. Two *different* compiler builds have still never been compared, so this is not yet "any Windows machine reproduces it". |
| Every external action is pinned to a full commit, named with its release, and pinned to one commit everywhere; Dependabot proposes updates | UNIT TESTED | `tests/test_workflow_pins.py:ActionPinTests` (6 tests), `ParserTests` (3 tests) | - |
| No job is granted write at the top level; the build job cannot write; the publish job runs no repository code; only a tag push publishes | UNIT TESTED | `tests/test_workflow_privilege.py:WorkflowPrivilegeTests` (8 tests), `KoSyncPrivilegeTests` (2), `YamlShapeTests` (1) | Workflow-file assertions; no run is replayed. |
| A published version's assets cannot be overwritten, and a dispatch cannot publish | UNIT TESTED | `tests/test_convergence.py:ReleaseImmutabilityTests` (5 tests) | The releases are not GitHub immutable releases; the rule binds the workflow, not everyone with write access. `docs/VERIFY.md` says so. |
| The release workflow runs the tests before publishing, refuses a tag that disagrees with the manifest, and verifies the published checksum | UNIT TESTED | `tests/test_plugin.py:ReleaseWorkflowTests` (6 tests) | - |
| The current version lives in one place, and the changelog leads with it | UNIT TESTED | `tests/test_plugin.py:VersionConsistencyTests` (4 tests), `ReleaseNotesTests` (6 tests); `tests/test_privacy_claims.py:WordingTests.test_no_released_version_has_lost_its_changelog_section` | - |
| CI runs every non-live test on Python 3.12, 3.13 and 3.14 as blocking jobs and on the 3.15 pre-release as an advisory one whose result does not block; the release still bundles a Python on a tested line | UNIT TESTED | `tests/test_python_support.py:WorkflowAgreementTests` (7 tests), `PolicyShapeTests` (6 tests), `BundledRuntimeTests` (3 tests) | The workflow file is checked, not run. A CI run is the evidence that the jobs ran, and this repository records none. |
| Released archives verify by digest, by the release page, by the digest pinned on `main` and, from v0.5.4, by build attestation | PUBLISHED | `docs/VERIFY.md`; `scripts/release.json`; `tests/test_convergence.py:ReleaseManifestTests.test_every_released_version_is_pinned`, `test_digests_are_absent_or_real` | For v0.6.0 the published archive was downloaded and hashed outside the build, and that one number agreed with the sidecar published beside it, with the digest GitHub reports for the asset, and with the subject of the build-provenance attestation - which names `.github/workflows/release.yml`, this repository and `refs/tags/v0.6.0`. The attestation's signature was then verified cryptographically, off GitHub, with `sigstore-python` rather than the GitHub CLI: the DSSE signature under the Fulcio leaf key, the chain to the Sigstore root, the embedded certificate-transparency timestamp, and the Rekor inclusion proof and signed checkpoint - with nine tamper controls showing each check refuses what it should. [`docs/evidence/attestation-verified-2026-09-13.json`](evidence/attestation-verified-2026-09-13.json) records it, including what it still does not show: the Sigstore root was trusted on first use, the bundle carries no separate timestamp authority, and provenance is not a build audit. |
| The brand assets and the icon are generated from their sources and stay current, and `gui/Brand.cs` carries the layout sizes, shadow recipes and glow numbers as single constants, with no array the in-box compiler would name at random, and a dark twin under the same name for every colour and rule that changes with the theme | UNIT TESTED | `tests/test_brand.py:GeneratedFileTests` (5 tests), `PaletteTests` (5), `SurfaceTests` (6), `GeneratedWindowTokenTests` (3) | - |
| Shadows, sizes and the status light are data in `src/codex_auto_resume/brand.py`: the shadow model reproduces the panel's CSS to two levels, `css_elevation` writes what the panel always wrote, an inset shadow falls inside the edges and an outer one outside, the hover and pressed accents follow their formulas and keep the button's text readable, every size is a whole pixel, and `brand.glow` is the light's one definition - cyan for every running state, the greys unchanged with no glow for paused, stopped and unknown, the alarms in their own colours, a system colour in High Contrast; a cycle that dims the dot and brings it back before anything spreads, a glow no more than 3 px past the dot on any surface and only round a lit dot, every hand-over at the still light, the approved preview reproduced, and every light lit and still with no glow with reduced motion | UNIT TESTED | `tests/test_brand.py:DerivedColourTests` (4 tests), `ElevationTests` (5 tests), `ScaleTests` (4 tests), `StatusLightTests` (18 tests, incl. `test_the_decided_numbers`, `test_nothing_spreads_before_the_dot_is_fully_lit`, `test_a_cycle_starts_and_ends_at_the_still_light`, `test_the_spread_reaches_three_pixels_past_the_dot_and_no_further`, `test_it_is_the_approved_preview`, `test_the_transition_eases_out_on_one_curve`); `tests/test_tray_icon_motion.py:StateTests.test_the_status_light_blinks_as_far_as_the_head_breathes` | The six panel pixels the shadow model is held to are written into the test; nothing measures a whole surface against the model. |
| Both themes and the check box are data in `src/codex_auto_resume/brand.py`: text reads at 7:1 and secondary text and the accent at 4.5:1 on every ground in both themes, state words at 4.5:1 on a card and the focus ring at 3:1 against each ground; the dark card's ground is the panel's `color-mix`, and its top light falls inside the card and its drops outside; the check box is the switch's knob in a hairline frame, names tokens both themes have, is a well unchecked and the accent with its mark checked, is told from its ground at 3:1 checked or not in both themes, and its tick is a mitred mark centred in the box; and the stylesheet gets it under names the panel can use | UNIT TESTED | `tests/test_brand.py:ThemeTests` (7 tests), `CheckBoxTests` (6 tests) | The contrast is worked out from the tokens, not measured on a screen. A disabled box is held to its mark's contrast only, and in dark an unchecked one is little more than its hairline. |
| The window's status light, generated into `gui/Brand.cs`, is `brand.py`'s: compiled and called, its glow, its timer rule and its dot colours give what `brand.glow`, `brand.glow_moves` and the palette give, and every generated constant reads back as `brand.py` wrote it; so do both themes' colours, each theme's dot fills and shadows, and the check box in each state and in High Contrast | REAL WINDOWS TESTED | `tests/test_brand.py:GeneratedStatusLightTests` (9 tests) compile `gui/Brand.cs` with the in-box compiler and call it through reflection for ten states at twenty-four moments - every phase of both cycles and their edges - and ten entry times, with reduced motion off and on, and read back `Brand` and `Brand.Dark` side by side | Skipped where the in-box compiler or PowerShell is absent, and there nothing notices the generated rules drifting from `brand.glow`: `GeneratedFileTests.test_gui_brand_cs_is_current` shows only that the file is what the generator writes, and the generator carries its own copy of the rules. |
| The release job checks every entry an installed bootstrap requires of an update, the payload root's files included; its list and the bootstrap's are held to each other in both directions and to what the published bootstraps require; the release carries the bundled compatibility data, and a payload without valid data stops the build | UNIT TESTED | `tests/test_plugin.py:ReleaseWorkflowTests.test_it_checks_everything_a_published_bootstrap_requires`; `tests/test_convergence.py:BootstrapTests.test_required_contents_match_the_release_workflow`; `tests/test_compat_surfaces.py:ReleaseTests` (3 tests) | The job's list is read from `.github/workflows/release.yml`, not run here. |
| The installation root is found by its manifest - at today's depth and one or two packages deeper, never past a foreign manifest or the bound - and a manifest that is there but damaged is logged once as a warning before the version reads unknown, while a missing one reads unknown quietly; the shipped manifest reads strictly | UNIT TESTED | `tests/test_config.py:ProjectRootTests` (7 tests), `VersionTests` (6 tests), `ShippedManifestTests` (1 test) | - |
| The safety scans read the whole package, recursively, through one tracked scanner, so moving code cannot take it out of a check's sight; the package's layers, its lazy imports and cycles, each module's size ceiling and the structural invariants are held; the screenshots are keyed on what the window is shown and what draws the popup, not on which files changed | UNIT TESTED | `tests/test_srcscan.py:ListingTests` (4 tests), `TreeTests` (4 tests), `NoOneFileScanTests` (2 tests); `tests/test_layers.py:LayerTests` (7 tests), `CycleTests` (2 tests), `LazyImportTests` (2 tests); `tests/test_sizes.py:SizeTests` (4 tests); `tests/test_structural_invariants.py:OneSenderTests` (5 tests), `PinnedPathTests` (3 tests); `tests/test_screenshots.py:EnvelopeTests` (6 tests), `PopupDrawingTests` (8 tests) | Nothing a user sees; these guard the split v0.6.6 carries. |
| A live-acceptance record is refused unless it is one: a missing field, a pass that observed nothing, a raw conversation id, a path, an e-mail address, a step the validator does not know, a version that is not the manifest's, or two files disagreeing about one step | UNIT TESTED | `scripts/live_evidence.py`, driven by `tests/test_live_evidence.py:WellFormedTests` (4 tests), `RefusalTests` (19), `DirectoryTests` (11) and `DocumentTests` (7, which hold `docs/LIVE_ACCEPTANCE.md` and the validator to the same steps and the same recorded values) | The validator checks the shape of what a person wrote and the vocabulary they wrote it in. It cannot check that any of it happened, and says so itself. |
| The live acceptance itself - install, watcher, Dashboard, a real interruption, cancel, retry now, giving attempts back, pause, upgrade, repair, uninstall, each on a real machine against a real Codex | UNVERIFIED | `docs/LIVE_ACCEPTANCE.md` says for each step what it proves, what to do, what a pass looks like and what to record; `docs/evidence/live/` is where the records go | **Nobody has run it for v0.6.5.** `docs/evidence/live/` holds a `README.md` and an `example.json` that records nothing and counts towards no step, so `python scripts/live_evidence.py` on this tree prints that nothing has been accepted yet and exits 2. The one run so far was of v0.6.0, kept under `docs/evidence/live-0.6.0/`: one step passed and fourteen were blocked, because no genuine interruption happened in the disposable conversation and none was fabricated. The checks for what v0.6.3 adds are in `docs/LIVE_ACCEPTANCE.md` too, written in step notes rather than as steps. Every row in this document that says a thing has never been watched in Codex is waiting on this procedure being run. |

## 17. The Codex Compatibility Registry

New in v0.6.5. What it answers is narrow - for the Codex engine on this machine, which of the
things this product does can be relied on - and the rows below are about the rules that keep
data from outside the release from making the watcher any less careful.

| Capability | Evidence | What backs it | What is still missing |
| --- | --- | --- | --- |
| Every answer is one of four states, by two rules: a failed local check always wins, and registry data can never raise a capability above what the local checks allow - it can restrict anything, and it can raise only a local pass, only to VERIFIED, only for an exact version | UNIT TESTED | `tests/test_compat.py:ResolveTests` (5 tests), `EvidenceTests` (4 tests), `EvaluateTests` (5 tests), `VocabularyTests` (3 tests) | - |
| A document is refused whole for an unknown major, a duplicate key, a number that is not finite, too deep a structure, a size over the cap checked before parsing, a range that grants trust, a VERIFIED entry without a citation or with a URL for one, and a signature it says it requires; unknown keys, capabilities and states are ignored and never read as VERIFIED; a reason outside the vocabulary becomes `unspecified`; and a clock that is behind only ever withholds trust | UNIT TESTED | `tests/test_compat.py:DocumentTests` (16 tests), `VersionTests` (2 tests), `ReportTests` (6 tests), `ViewTests` (4 tests) | - |
| The bundled data validates, cites only files that exist, names no host, is small and ASCII, and ships no VERIFIED entry until a recording states the version it was made on | UNIT TESTED | `tests/test_compat.py:BundledBaselineTests` (6 tests) | No recording under `docs/evidence/` states its Codex version yet, so nothing can be VERIFIED. |
| The validator is the cache's one writer: a valid document becomes the cache, a refusal leaves the cache exactly as it was, a document older than the bundled data is a rollback, a tampered cache is ignored and kept, a crash mid-write leaves the old file and no temporary one, and uninstall owns both files | UNIT TESTED | `tests/test_compat_io.py:ImportTests` (8 tests) | - |
| The watcher writes a report bound to its engine, and every reader refuses one about a binary that has changed or vanished, one older than a ticking watcher leaves, and a damaged one; an imported advisory blocks new sends and touches nothing already sent; a failing evaluation fails closed and never ends the watcher; it recomputes only when something changed, with one content-free log line per change; and a cache that expires, or a clock that is behind, only withholds trust | UNIT TESTED | `tests/test_compat_io.py:EvaluatorTests` (12 tests), `TimeTests` (4 tests), `SourceCheckTests` (6 tests), `LiveTests` (2 tests) | Against a Codex-shaped fixture home, not a real Codex. |
| With the bundled data the engine gate decides what it decided before the registry: the formerly verified build and an unverified one that still offers the queue flags send, a build without them or whose `codex queue --help` fails is refused, no engine is unknown, a binary that vanishes or an in-place update that drops the flags waits for the app as before, and a missing projection table still blocks with its own reason | INTEGRATION TESTED | `tests/test_compat_characterization.py:EngineGateCharacterizationTests` (9 tests) run the real discovery, backend check and engine over a real store and a simulated Codex home; `FixtureHermeticityTests` (2 tests) | A scripted fake `codex.exe` at the path Windows resolves to, not a real Codex build. |
| The refresh is one GET to one constant URL with nothing about the machine in it, on the one host allowed for it alone; a download from any other host, one over the cap or one the validator refuses is not kept, and none outlives the refresh; no network is an answer, not an error; the host is named before it is asked; with no installation or no time left nothing is asked; on an update check it runs only after github.com answered, before the update's own answer and inside the time left, and a check that could not ask github.com does not refresh either | REAL WINDOWS TESTED | `tests/test_compat_bootstrap.py:RefreshFunctionTests` (10 tests) and `WholeScriptTests` (5 tests) run the bootstrap's own functions and the whole script in the real Windows PowerShell, with the download stubbed or pointed at a port nothing listens on, and the real validator; `UpdateCheckTimeTests` (3 tests), `UpdateCheckWholeScriptTests` (2 tests), `ScriptTextTests` (5 tests); `tests/test_compat_surfaces.py:PrivacyTests.test_the_refresh_url_is_one_constant_with_nothing_about_the_machine_in_it`, `test_the_registry_modules_import_no_networking_module`; `tests/test_compat_io.py:RefreshTests` (3 tests) | Skipped where PowerShell is absent. By design no test reaches raw.githubusercontent.com, and nobody has recorded a real refresh. |
| The bridge's `compatibility`, `compat-import` and `compat-refresh` answer with codes, never echo a file, and never show a binding; the diagnostics bundle carries the view with no binding | UNIT TESTED | `tests/test_compat_surfaces.py:BridgeTests` (7 tests), `DiagnosticsTests` (1 test) | - |
| PRIVACY.md and SECURITY.md, and their Korean pairs, name the refresh, its host, when it happens and both files, and the item for `get_status` names the summary as codes | UNIT TESTED | `tests/test_compat_surfaces.py:PrivacyTests.test_the_documents_name_the_refresh_before_it_ships`, `test_privacy_names_the_compatibility_summary_get_status_returns` | Shape-based: names and nearby words, not sentences. |

---

## Where the evidence is thinnest

These are the capabilities this product claims that nothing in this repository exercises,
gathered so they are not lost among the rows that are well covered.

| Capability | Level | The smallest thing that would raise it |
| --- | --- | --- |
| "Unreadable, not empty" in the window | REAL WINDOWS TESTED, without a picture | A capture of the Pending page taken with the bridge made to fail. |
| The five-second refresh | IMPLEMENTED | Anything at all that measures it. |
| A real `Run` key at a real sign-in | UNIT TESTED, against a fake registry | A `logs/launcher.log` captured after a Windows sign-in. |
| A real protocol registration and a real toast click | UNIT TESTED, against a fake registry | The same, plus a recorded activation. |
| Open Dashboard on a notification | IMPLEMENTED | A test that builds, parses and activates the open URI, and a click on a real toast noted during `docs/LIVE_ACCEPTANCE.md` step 4. |
| The icon coming back after Explorer restarts | IMPLEMENTED | Restarting Explorer once with a watcher running, and noting that the icon came back. |
| The popup on a real notification-area icon | REAL WINDOWS TESTED, as pictures rendered off-screen (`docs/images/tray-popup.png` and its ko, ja, zh-CN and de siblings) | A recorded live check that opens it from the icon, closes it with Esc and click-away, and uses its switch on a real waiting task. |
| The window's speed | UNIT TESTED, as source shapes | An instrumented measurement of the installed build - the first painted header and the page and section switches - kept under `docs/evidence/`. |
| High Contrast in the window, the popup and the panel | UNIT TESTED, with High Contrast forced | A look at each in a real High Contrast theme, noted while running `docs/LIVE_ACCEPTANCE.md`. |
| The window reopening itself after a change of language or theme | REAL WINDOWS TESTED, as its own decisions, with no handover run | A recorded live check that saves a new theme in the window, changes the language from the panel with edits unsaved, and switches Windows' app mode with the window open. |
| The dark theme on a real desktop | REAL WINDOWS TESTED, as colours and pixels read back, with a picture of the notification card only | A capture of the window and the popup in dark, and a look at the panel in Codex's dark theme. |
| The dark right-click menu | UNIT TESTED, against a fake uxtheme | A look at the real icon's menu in dark, on Windows 11 and on Windows 10, noted while running `docs/LIVE_ACCEPTANCE.md`. |
| The live acceptance procedure | UNVERIFIED | Somebody runs `docs/LIVE_ACCEPTANCE.md` on a real machine and commits the files it produces under `docs/evidence/live/`. |
| The notification card on a real desktop | REAL WINDOWS TESTED, off any person's view, and as pictures rendered off-screen (`docs/images/notification-card.png` and its siblings) | A person watching cards arrive, stack and go, over a real wallpaper and beside a real taskbar, and the silent copies in the notification center, noted while running `docs/LIVE_ACCEPTANCE.md`. |
| The compatibility refresh against GitHub | REAL WINDOWS TESTED, with the download stubbed | One press of Refresh compatibility data on a real installation, and one Check for updates, with their answers and the cache they wrote recorded. |
| The icon's motion | UNIT TESTED, and pictured in the README by a GIF drawn from the icon's own frames | A recording of the notification-area icon itself breathing and turning on a real taskbar, and holding still under Reduce motion. |
| Anything at all seen inside Codex | - | Nobody has watched this product work in Codex. Until somebody does and the repository records it, no row here can say REAL CODEX VISUALLY TESTED. |

## What a reader should take from this

The recovery engine is the best-evidenced part. Its detection, its gates, its correlation,
its outcomes and its budgets are driven end to end against a real Codex-shaped database
through the production query layer, and the three facts the whole design rests on - the
usage-limit shape, delivery to a loaded conversation, non-delivery to an unloaded one -
were measured against real Codex on 2026-09-06 and are published under `docs/evidence/`.

The surfaces are the weakest. The window's six pages are real code, captured from the compiled
window in the light theme only; its dark theme and its reopening are driven through its own
code and never watched. The notification-area icon and its popup are created and destroyed for
real in a handful of tests, off-screen; the popup's and the notification card's pictures are
drawn by their own code, and nothing shows the icon. The Codex panel has never been loaded by Codex, though its request
builders, its theme and its switch of language now run in Node.
Where a surface talks to the control layer, the control layer is well tested and the
surface's own half usually is not.

And nothing here has been watched. That is the honest summary: a carefully tested program
that, as far as this repository records, nobody has yet sat and watched recover a
conversation.
